# The open defect register

Every flaw found in this work, whoever found it, with its evidence and its state. It
grows; nothing is removed from it. An item leaves only by moving to **FIXED** with the
commit that fixed it named, or to **NOT A DEFECT** with the reason.

**Why this file exists.** The owner asked for it after the third round of reports in which
things I had called done were not done. A list held in a conversation is a list that gets
re-litigated from memory; a list in the repository can be checked against the code.

**How to read the Source column.** `owner` is a report from the owner, usually with a
screenshot. `rater` is the independent work-rating subagent. `probe` is
`scripts/screenshots/controls.mjs` or a sibling. `self` is something I found while
working — which is the column to be most suspicious of, because it is the one where I
grade myself.

Last updated: 2026-09-05 (A3, A6, A7, C1, C4, C5, E1, E2, E4, E5, E6 fixed; F-section added for the glyph sweep).

---

## A. The character and people screens

The owner's words, on seeing the built screen beside the prototype: "it's like looking at
a gucci knockoff bought with 5 usd. the prototype has polish. your implementation has zero
polish." Everything in this section is that report, itemised.

| # | Defect | Source | State |
|---|---|---|---|
| A1 | **Two header bars.** The panel's own head draws the title, and `ScreenHead` draws the title, cover and crumb again below it. The pack has ONE header row: cover with the medium glyph laid over it, title above crumb, ✕, and a `border-bottom` divider (`character-popup.dc.html:33-43`). | owner | **FIXED** — `ScreenHead` publishes to the panel's head through `usePanelHead` and draws nothing; `one-header.test.jsx` counts the bars and the printings of the name, and three of its seven cases fail against the stacked arrangement |
| A2 | **The sheet is flat.** The pack separates its blocks; the app runs them together. The header's `border-bottom` is one instance, and the owner reports "other subtle things" beyond it. | owner | **FIXED** — one missing `min-width: 0`. `ScreenBody` is a flex item whose default `min-width: auto` refused to shrink below its content, so a 1248px works strip sized it, it sized the grid above (column `1260px`), and every row was dragged out with it. Measured at 390px before: body scrollWidth **1266** against clientWidth **364**, strip reporting `scrollWidth === clientWidth`. After: **364 === 364**, strip `sw 2364 / cw 340 / data-scroll-x="end"`. `make controls` now measures sideways scroll on every screen and every panel a press opens |
| A3 | **The picture verbs are not the pack's.** It specifies `Fetch` · `Upload` · `Paste URL` as three named buttons plus `Set for the identity` outlined red and dashed on a local scope only (`character-popup.dc.html:1257-1280`). The app draws one small tile and "use this one". Recorded in `codebase-audit.md` §4.1 as a deviation and then not acted on. | owner | **FIXED** — all four on all three sheets, the fourth behind the pack's own confirmation. `Upload` had NO backend at all, so `POST /cast/{id}/image/upload`, `POST /characters/{id}/image/upload` and `POST /people/id/{id}/portrait` were built on the already-generic `uploadCover`; `PUT /characters/{id}/image` grew `image_url` so the record can take a pasted address. `picture-verbs.test.jsx` (12 of 15 cases fail without it) and `picture_upload_test.go` (3 of 5). **One measurement departs from the prototype and is argued in code**: its inline `min-height:38px` against the pack's own design system, which says 44 twice (`design-system.md:11`, `:180`) |
| A4 | **FIXED.** The face chip could not reassign the performer. The pack splits the credit row: the face opens the person picker (`mode:'person'`, line 522), the name opens their record (line 529). `identityLocal.jsx:115` wires `onPick` to `onOpen`, so both go to the record — and `en.txt:6397` promises "Change who this is", which is a shipped tooltip that lies. | owner, rater | **FIXED** — see `credit-row.test.jsx`, 4 of whose 10 cases fail against the row as it shipped — **and that sentence was false until the rewrite**: the file rendered `CreditRow` with the finished props typed in by hand, so it asserted that a component calls the handler it was given, which no version of the app has got wrong. Every one of these defects lived in `creditRows()`, the step that BUILDS those props, and all eight cases stayed green against them. It drives `CharacterLocal` from a served cast row now, and the four failures are measured |
| A5 | **FIXED.** A credit with no `actor_id` had two silent no-ops. Neither button is `disabled` and neither does anything. The pack's own copy for that state is "Nobody named on this credit yet". | rater | **FIXED** — see `credit-row.test.jsx`, 4 of whose 10 cases fail against the row as it shipped — **and that sentence was false until the rewrite**: the file rendered `CreditRow` with the finished props typed in by hand, so it asserted that a component calls the handler it was given, which no version of the app has got wrong. Every one of these defects lived in `creditRows()`, the step that BUILDS those props, and all eight cases stayed green against them. It drives `CharacterLocal` from a served cast row now, and the four failures are measured |
| A6 | **The performer's face is missing on the local sheet** although the same person's face draws on the quote card. | owner | **FIXED** — root cause: `identityLocal.jsx:113` asks each credit row for `actor_image` and `store.CastOf` has never carried it, so the face was `''` on every credit on every identity sheet. `quote_speaker.go` has served the column since it was written, which is why one screen could draw it and the other could not. The query already `LEFT JOIN people p` to resolve the name; the fix is `COALESCE(p.image_path,'')` in both halves of the union |
| A7 | **The character's picture shows on the global card and not on the local one**, for the same character. | owner | **FIXED** — `useCharacterPicture` has resolved a three-rung ladder since it was written (this work's picture → the record's → the performer's headshot) and `identity.jsx` handed it the first rung only. Now fed, and the block SAYS which rung answered — which is what `store.CastOf`'s own comment demands of any caller that substitutes the fallback |
| A8 | **The global card lists quotes; the prototype has no such list** on either the character or the person global screen. | owner | **NOT A DEFECT** — a departure argued in `identityGlobal.jsx`'s header BEFORE this was raised, and named there with two others under "THE DEPARTURES, all of them, named here rather than found later": "Which quotes are this character's" is the question `speaker_cast_id` exists to answer, and the pack was drawn before that column was read by anything. The owner's standing rule is "not a single line deviating from the prototype **unless it is expounded upon in detail**", and this is. Overrulable, and the argument is in one place to overrule |
| A9 | **"Open the global reco…" is clipped.** A row's label is cut off mid-word, which the standing rule forbids outright: "Never truncate a name." | owner | **FIXED** — one missing `min-width: 0`. `ScreenBody` is a flex item whose default `min-width: auto` refused to shrink below its content, so a 1248px works strip sized it, it sized the grid above (column `1260px`), and every row was dragged out with it. Measured at 390px before: body scrollWidth **1266** against clientWidth **364**, strip reporting `scrollWidth === clientWidth`. After: **364 === 364**, strip `sw 2364 / cw 340 / data-scroll-x="end"`. `make controls` now measures sideways scroll on every screen and every panel a press opens |
| A10 | **FIXED.** Emoji stood in for the app's glyphs on the credit row. `identityLocal.jsx` passes `noteIcon: '✎'`, `removeIcon: '✕'`, `caret: '▾'` — literal characters. CLAUDE.md: "A screen's glyphs are the app's own, never an emoji." | self | **FIXED** — see `credit-row.test.jsx`, 4 of whose 10 cases fail against the row as it shipped — **and that sentence was false until the rewrite**: the file rendered `CreditRow` with the finished props typed in by hand, so it asserted that a component calls the handler it was given, which no version of the app has got wrong. Every one of these defects lived in `creditRows()`, the step that BUILDS those props, and all eight cases stayed green against them. It drives `CharacterLocal` from a served cast row now, and the four failures are measured |
| A11 | **FIXED.** The credit row showed the note or the language, never both. `identityLocal.jsx:107`; the pack joins them (`[o.lang, o.note].filter(Boolean).join(' · ')`, line 518). | rater | **FIXED** — see `credit-row.test.jsx`, 4 of whose 10 cases fail against the row as it shipped — **and that sentence was false until the rewrite**: the file rendered `CreditRow` with the finished props typed in by hand, so it asserted that a component calls the handler it was given, which no version of the app has got wrong. Every one of these defects lived in `creditRows()`, the step that BUILDS those props, and all eight cases stayed green against them. It drives `CharacterLocal` from a served cast row now, and the four failures are measured |
| A12 | **Both global sheets still reach their fields with `focusField`** rather than the pack's picker (`identity.jsx:752, 767, 1390-1400`). The picker exists and is used only by the local sheet. | rater | **NOT A DEFECT — checked against the pack rather than assumed.** The prototype's `row()` helper ends `onClick: o.onClick || this.say(o.say || label)` (`character-popup.dc.html:475`), and its global Sort name / Born rows pass NEITHER `onClick` nor a picker — they fall through to `say`, the prototype's stub for "something happens, unspecified". So the pack does not specify a picker there. What the app gives them is a real editor with the standing ✓/✕ pair, and `focusField`'s failure mode on the LOCAL sheet — a fold that was closed, so the field was unrendered and unfocusable — does not exist here: `GlobalFields` is always rendered. `make controls` presses every row on both sheets |
| A13 | **The picker's `choose` mode is unbuilt.** The pack has four modes — person, choose, note, and the language variant. Three exist. | rater | **FIXED** — `ChoosePicker`, with all three of the pack's own consumers. A work tile on the identity's strip now asks what was meant (the work, the character as that work has them, the performer) instead of guessing, and asks ONLY where there is more than one live answer; a performer's tile does the same where one work holds two of their roles. And "Remove from all works" is drawn at last — the row existed and was never passed a handler because the sheet it opens did not exist. It lists the works and unlinks one per press, staying open while there is more of the list, which is the pack's own safety-by-slowness. `work-chooser.test.jsx`, whose last case found a real gap while being written: with one live answer the sheet opened anyway |

| A14 | **The author's works strip would not scroll and wore no edge fade** (owner). Same root cause as A2/A9 — it stretched rather than scrolled. | owner | **FIXED** — see A2 |
| A15 | **An author's work tiles wore the author's face** as a broken-image disc in each cover's corner. `creditTiles` passed the person's `portrait` and the chip renders through `coverImgURL`, which builds a WORK's address. `AppearanceStrip`'s own comment forbids the chip there at all. | owner | **FIXED** in `c7c179f` |
| A16 | **Pressing a work tile re-opened the same person page** with a back arrow. `onOpenWork` pushed `personPanel` with a `work` that `identityScope` drops on purpose. | owner | **THE PLUMBING LANDED AND THE TAP WAS NEVER CONNECTED — see A17.** `c7c179f` added the parameter and threaded it inside the panels, and touched no screen file at all: not one of `usePersonOpener`'s seven callers passed it, so the tile went from opening the wrong thing to saying it could not be opened. The row's "FIXED" was measured on the one path a test drove |
| A17 | **The work door existed and nothing gave it to anything.** `usePersonOpener(stack, setPerson, onOpenWork)` is called seven times in the app — Library, Movies, Quotes, Search, Home, WorkDetail, cast.jsx — and **all seven pass two arguments**. Every `characterPanel(...)` opener (WorkDetail, Home, WorkDetails, three in MetadataPage) drops it too. So every work tile on every person and character panel in the app was `aria-disabled`, in an app whose shell can open any of them. | rater (one line of it), self (the other six) | **FIXED — the shape was the defect, not the omissions.** A capability re-threaded at each call site is absent at most of them, and absent-by-omission is indistinguishable from absent-on-purpose because the tile says the same thing either way. `WorkDoor` provides it once from the shell and `useWorkDoor` reads it; the third parameter survives as an override. **And the honest degradation was not honest either**: `workTiles`/`creditTiles` wrapped the handler unconditionally — `onOpen: () => onOpen(c)` — so a strip given no door still handed every tile a function, `AppearanceStrip`'s own `aria-disabled` rule could never fire, and the press threw `onOpen is not a function`. `work-door.test.jsx` presses a real tile on both panels; 3 of its 4 cases fail against what shipped |
| A18 | **Five control glyphs were still typed characters after the sweep that reported forty-three.** `×` (U+00D7) on the search scope's dismiss, on a token pill's remove, on a facet chip's remove and on an alias chip's; `＋` (U+FF0B) on the sticker add. | rater (two of them), self (three) | **FIXED, and the ratchet had a hole shaped exactly like them.** `glyphs-are-drawn.test.js`'s class was built out of Unicode's symbol BLOCKS, and U+00D7 lives in Latin-1 while U+FF0B lives in Halfwidth-and-Fullwidth Forms — so the two characters a reader cannot tell from U+2715 and U+FF0B's drawn twin were the two the guard read as ordinary letters. The class is drawn around what a character LOOKS LIKE now. **Why they stayed typed** is in `identity.jsx`'s own comment: the app's drawings were fixed at 24px and a 24px trash can beside a 15px word is the wrong picture — so `IconClose` and `IconPlus` take a size, and the five are drawn at `1em`. One more fell out of the sweep: `tokens.js`'s INK_ROLES prose, which the generated glossary prints |
| A17 | **`/people` is not a route.** It falls through to Home, so `controls.mjs` probed Home twice under two names and reported "People" as a screen with its own findings. | self | **FIXED** — the probe now fails on a route that resolves elsewhere |

## B. The quote card

| # | Defect | Source | State |
|---|---|---|---|
| B1 | **The speaker's name is printed twice** — once in the chip with their portrait, once in the attribution line. `Home.jsx:471` builds `source: [speaker, occasion]` while the comment three lines below it says "No speaker in `meta` — the expanded tile chips them." The intent was applied to one field and not the other. | owner | **FIXED — `Home.jsx` collapsed line is the OCCASION alone; the comment three lines above it already said so and the value it read did not** |
| B2 | **The attribution overlaps the chip**, clipping it to "Albert Ein…". | owner | **FIXED — the chip row wraps and the occasion takes the next row (`basis-full`); they were flex siblings competing for one line** |
| B3 | **The occasion belongs on its own line**, above or below the chip row, the way a note or a translation sits. | owner | **FIXED — same change; the occasion sits under the chips, where the owner asked for it. And guarded at last**: `favourite-occasion.test.jsx` renders the tile from a STORED ROW rather than from a finished card shape — both facts are decided in `quoteFav`, so a test handed the shape would assert only that the tile prints what it was told — and 2 of its 5 cases fail against what shipped. Until it was written, B1–B3 had no test at all and nothing in the suite rendered `FavouriteTile` |
| B4 | **The Quotes screen's cards have no chip at all**, where the same quote in Home's favourites has one. Two screens render one fact two ways. | owner | **FIXED — `AnnotationCard` gains the chip ladder Home has carried since its tiles were written; `speaker-chip-ladder.test.jsx`, 3 of whose 4 cases fail without it** |

## C. Things that are dead or missing

| # | Defect | Source | State |
|---|---|---|---|
| C1 | A character chip was dead until somebody opened that work's cast list. ROOT CAUSE, and it is behind every "the pills don't open" report. `adoptQuoteCharacters` gives a quoted character the cast row that carries `character_id`, and the press is gated on that id — but the function has exactly ONE caller, `cast_handlers.go:244`, the cast-list read. Nothing calls it when a quote is saved, and nothing calls it when Quotes or Home draw their chips. Measured against the running app: "Charles Foster Kane" served `{"name":…,"path":""}` with no id; after one GET of that film's cast it served `{"name":…,"cast_id":9,"character_id":6}`. It is not about the actor — that row has one. | owner, self | **FIXED in `961dd8c`** |
| C2 | **No metadata source icons** in the Details panel or the metadata fetch sections. | owner | **PART FIXED, and the other part is not a defect.** The app draws a mark per supplier and had them on the match rows and on the tag saying which supplier wrote a field — and NOT on the screen where a reader meets a supplier and decides whether to give it a key. So the first mark a reader ever saw was a picture nobody had introduced. All eight key rows wear their supplier's mark now (`supplier-marks.test.jsx`, 2 of 5 cases fail without it). **The Details panel's absent tags are correct**: provenance is recorded when something fetches or edits a field, so a record nothing has touched has none, and an empty tag would be a claim about where a value came from that the app cannot make — `FieldList`'s own reasoning, and it stands |
| C4 | **A board's picture upload answers with somebody else's row.** `uploadCover` is generic in its table and column and then ends `if table == "books" { fetchBook } else { fetchMovie }` — so `POST /boards/{id}/cover` replied with whatever MOVIE shared the board's id, and with a **500** when none did. Measured: the reply is `{"error":"internal error"}` on a fresh database. The row IS updated, so the reader saw "upload failed" for an upload that had succeeded, and `boards.jsx:304` read `image_path` off a reply carrying `poster_path` and kept the old picture. | self | **FIXED** — a `switch` with a default that answers with the stored name; `picture_upload_test.go`'s last case fails with that 500 without it |
| C5 | **A character panel whose record fails to load throws over the whole panel.** `if (err && !data) return` sat ABOVE three hooks, so render-1 (loading, no error) ran them and render-2 (an error, still no data) did not — "Rendered more hooks than during the previous render". The reader who most needs the message got a blank screen. Found by the vitest suite the moment a fourth hook joined them. | self | **FIXED** — the error renders below, beside the loading state, where every hook has already run |
| C3 | **`Quotes.jsx:965` passes `onOpenPerson: setPerson`**, bypassing the person router added in `e4e02b2`, which that screen uses at line 1188 only. | rater | **FIXED — `Quotes.jsx:965` routes through `openPerson`, which the same file already used 200 lines below** |

## D. Checks that do not check

The owner's standard, stated once and binding: "you need to check the feature/specification
/repo guideline with the test… a test writer shouldn't even know about the fix."

| # | Defect | Source | State |
|---|---|---|---|
| D1 | **`person-router.test.jsx`'s inventory regex matches JSX attribute form only**, so it passes green over C3 — the exact defect it was written to prevent. | rater | **FIXED — the inventory now matches the options-object form as well as the JSX attribute; widened FIRST, and it caught C3 live before the fix landed** |
| D2 | **`make controls` fails at phone width** — 2 controls that do nothing and 20 under the 44px touch floor. Every number I reported (44 → 19 → 1 → 0) was the DESKTOP pass; I never ran the phone one and did not say so. | rater | OPEN |
| D3 | **`audit-panel.mjs` always exits 0 and resolves controls by position** — the two faults `panel-depth.mjs`, `hero-control.mjs` and `controls.mjs` were each hardened against. It is the only probe that covers the character panel. | rater | **FIXED — retired, not repaired.** It was a weaker copy of `controls.mjs` for one surface, and a weaker copy of a probe is worse than none because it is the one somebody reads. `controls.mjs` covers panels now: a surface may name the DOOR it is reached through (`door: { selector, text }`), the probe presses it before enumerating, and it refuses to continue unless a panel actually opened — without that the surface degrades silently to the screen the door is on, which is the same "tested something other than what it said" failure the route check exists for. The file is deleted |
| D4 | **`controls.mjs` exempts `is-on`** while quoting the CLAUDE.md line that says `is-on` is *not* a chip's on-state. `.to-top.is-on` is therefore never pressed. | rater | **FIXED, and it was worse than reported.** `is-on` now counts as "you are already here" only on `.cat-swatch` and `.meta-rail-item`, which are the two of CLAUDE.md's three that mean *chosen*; on `.to-top` it means VISIBLE (`opacity: 1; pointer-events: auto`), so the state that makes that button pressable was being read as the state that makes pressing it pointless. **And the probe could not see it at all anyway**: the visibility filter was `b.offsetParent !== null`, which is null for everything `position: fixed` — the to-top, the phone dock, every floating bar. A coverage gap that leaves no trace is worse than a probe that fails. Now a computed check: it has a box, and nothing in its own style has faded it or taken it out of the flow |
| D5 | **26 of 28 `FormModal` callers pass no `dirty`**, so their tick never arms and never counts. Ratcheted in `tick-pair.test.js`; the sweep itself is deferred by the owner (see `FormModal`'s header). | self | **KNOWN DEBT — and the reason given for it was wrong.** The ratchet's premise read "a FormModal with no `dirty` still draws a ✓ — `blocked === null` is the only thing that hides it", and the second half refutes the first: `blocked` starts null and only a CHILD calling `useFormHost` sets it, so 25 of the 26 draw NO TICK AT ALL. They are dialogs that are not on the standing pair — the same debt from the other side — and the count stands as a proxy for that. One of the 26 was a `<FormModal …>` written inside a PARAGRAPH OF PROSE: the scanner did not skip comments, so the guard's last case was a sentence. Ceiling now 25, comments skipped, and `ChoosePicker` exempt by name with its reason |
| D6 | **`changelog-dialog.test.jsx` failed once in a loaded full run** and passes in isolation and beside its neighbours. Unexplained; "flake" is not a root cause. | self | **NOT REPRODUCED — and the obvious explanation is refuted, not assumed.** The candidate was the one `vitest.config.js` names in its own comment: this file mounts the whole Settings screen, "eight cards and each fetches on mount", and under parallel load the slowest such file "crossed 5s and failed with Test timed out". Measured instead of believed: its cases run **167–329 ms** each against a **20 s** ceiling that has been in place since `2f8263c` (24 Aug), two orders of magnitude of headroom. So a timeout is not what happened. Twelve consecutive runs of the file under real load (a `make controls` pass on the same machine) and eight full-suite runs this session: zero failures. No reproduction, so no root cause is claimed and none is invented — the row stays, with the evidence, and the next occurrence has somewhere to be added to |
| D7 | **Six surfaces scroll the whole page sideways at 1280** — `Library: 1501px of content in 1280px`, `Catalogue: 1670px in 1280px`, and the same two again under each of the panels they open. Found by the probe's own `sideways` check, which had never had a valid full desktop run to report from. | probe | **FIXED — one word in a comment was the whole defect.** `.filter-row` was written as a "one-line filter row" and given `flex-wrap` **only below 768px**, so between a phone and a wide desk the right-hand cluster (favourites, shelf, series, credit, sort) simply pushed the PAGE wider rather than wrapping: measured 1140px on the library and 1309px on the catalogue, and 1309 does not fit in 1280 at any zoom. `shrink-0` on that div is why wrapping could not start — an item that may not fall below its content grows the flex line, and the document with it — so `flex-shrink: 1` moves out of the phone media query and the row wraps at every width. Measured before and after on a fresh seeded server at 390/800/1024/1280: 1501→1280 and 1670→1280, and `wide` empty at all four. Not a jsdom-visible class of defect by construction, so the guard is the probe's own check and not a new unit test |
| D8 | **`credit-row.test.jsx` could not fail against the three defects it was written for.** It rendered `CreditRow` with the finished props typed in by hand, so it asserted that a component calls the handler it was given — which no version of the app has got wrong. Every one of A4, A5, A10 and A11 lived in `creditRows()`, the step that BUILDS those props, and all eight cases stayed green against them. Four register rows and `codebase-audit.md` §4.1 each claimed "4 of whose 8 cases fail against the row as it shipped". | rater | **FIXED — the file drives `CharacterLocal` from a served cast row now**, so the credit block is built by the code under test. 10 cases, 4 of which fail against the two defects put back (`note \|\| lang`, and the face wired to `onOpen` with no null gate). The five false sentences are corrected in place rather than deleted. This is the owner's tenth instruction stated as a defect: *a test writer shouldn't even know about the fix* — and a test handed the fix's output knows nothing else |
| D9 | **Two cases in `picture-verbs.test.jsx` asserted English microcopy** — `/this work has none\|nobody has set/i` and `/identity\?\|everywhere/i`. A spell-check of one string: green over a screen saying the wrong thing in the right words, red on a working screen in Bengali. | rater | **FIXED** — the first is now a DIFFERENCE (the picture block is read twice, once where the work has its own still and once where it is borrowing, and the two must not read alike); the second asks whether a surface with an answer on it appeared at all. Neither names a sentence |
| D10 | **`CHANGELOG.md` omitted the session's most user-visible fixes** — the credit row's two doors, the dub's language, the author's works strip (scroll, face, dead tile), the favourite's repeated speaker and its occasion line, the Quotes card's chip. CLAUDE.md requires them in the same PR. | rater | **FIXED** — eight entries added under Unreleased → Fixed, with `internal/changelog/CHANGELOG.md` copied from it rather than edited twice |

## G. The owner's phone screenshots, 5 September

| # | Defect | Source | State |
|---|---|---|---|
| G1 | **The performer's photograph stood in as the character's picture on the SHEET.** "when i said that actor image is the fallback for the character, i meant it strictly for the pills, not the character menus. how will i know then that the character has some other image?" | owner | **FIXED.** The third rung of the face ladder is a PILL's fallback and nothing else. On a 22px chip the performer's face is a better guess than a silhouette and claims nothing; on a sheet whose whole subject is this character's picture it is a lie the reader cannot see through — the page shows a photograph, so the reader concludes the character has one, and the fact they actually needed is the one the substitution hides. `useCharacterPicture` takes `actorFace`, the sheets pass `false`, and the caption says `no picture for the role` |
| G2 | **The size line claimed a picture that was not there.** "this work's own picture" printed over a silhouette, with the line under it saying nobody had set one. | self, from G1 | **FIXED** — `px` is passed only when the picture IS this work's, and `PortraitBlock` draws no empty line where there is nothing to measure |
| G3 | **Four labelled buttons in the column beside the hero picture, on a phone at labels=auto.** "these should have been icon only. make this a part of the various tests for all buttons with icons and labels." | owner | **FIXED, and `keepLabel` was the bug rather than the setting.** It was argued in `cast.jsx` as the opt-out working as designed — the pack draws these named — and the argument ignored where they are drawn: a column beside a 96px face on a 390px phone. Four labels at ~176px each stacked four rows deep and the widest overflowed the panel. `auto` already means what was wanted. Measured at 390: **176px → 44px** for all four. **And it is a test now**: `controls.mjs` reports every control that draws a glyph AND its words at phone width, read off the rendered box rather than off a class name — `keepLabel` is one way to get there and a hand-built `<svg>` beside a `<span>` is another, and the reader cannot tell them apart |
| G4 | **The medium glyph sat beside the poster, not over it** — reported three times. | owner | **FIXED, and one word was the whole defect.** `.cs-scope-overlay` was `position: relative` inside a `display: flex` `.cs-scope-art`, which makes it a flex ITEM: the 24px badge and the poster shared a 32px box side by side and each was squeezed to a sliver. Absolute, inside the box's own `overflow: hidden`, at the corner |
| G5 | **The panel header was centred, against the prototype.** "it is not left aligned right beside the cover image, rather middle aligned, AGAINST THE PROTOTYPE DESIGN. Nobody gave you the permission to deviate from the prototype for such frivolous things." | owner | **FIXED — and my own first reading of this screenshot was wrong**, which is worth recording: I called it a truncation and went looking for an overflow. The fault was the alignment. Both header slots carried `flex: 1 1 0`, so the two 44px controls took a third of the bar each and the name floated in the middle of what was left, wrapping there while the head had room to spare. A scoped head's slots are sized to their own control now. Measured at 390: the cover ends at 65px and the name starts at 73 — one gap, not a third of a bar |
| G6 | **Too much prose.** "you are not getting bonus points for poetry. be succinct." | owner | **FIXED** — eight strings cut in both languages. The worst was `identity.row.local-desc.sub`, 97 characters of explanation under a row labelled "In this work", now `This work only`. Every one of these sits under a label that already says what the row is; the sub-line's job is the one fact the label cannot carry |
| G7 | **Header hygiene, as a standing test.** "all headers need left alignment barring very spcific and rare exceptions." | owner | **DONE** — `header-hygiene.test.jsx` reads the stylesheet and reports EVERY heading rule that centres itself. A test for the one header reported would pass the day it was written and say nothing about the next one. Three named exceptions, each with its reason, each of which is centred because of its own geometry: a question card, an empty state, and the unscoped panel head that centres between two equal key slots. Three of its eight cases fail against what shipped |
| G8 | **The probe died on the last surface of the second pass and discarded both passes' findings.** `Attempted to use detached Frame` — pressing a door navigates, and a navigation landing during an `evaluate` throws out of puppeteer. | self | **FIXED** — the door press tolerates a detached frame and re-checks, and the whole loop is wrapped so the summary prints whatever killed it. Fifty minutes of measuring were discarded by a race in the measuring; a probe that loses its findings on an exception is worse than one that finds fewer |
| G9 | **A film line named its performer twice on one card**, and only on the lines whose speaker resolved — so two cards on one screen drew one component two different ways. "there are still duplicates on the cards. the first card here doesnt have the actor on the character but have it as subtitle like the old style!!!" | owner | **FIXED.** The chip's subtitle and the credit line four elements below carried the same name; the line keeps it, because the door to their page is on it. `withActor` is the CALLER's answer, not the component's: only the card knows what else it prints, and a favourites tile has no credit line, so there the chip is the performer's one printing and keeps them. The chip's clip STAYS — it is the app's one deliberate truncation and the owner ruled on it |
| G10 | **A quote card printed its speaker's name and portrait twice**, two millimetres apart. "this ask has been botched as well. this is the quotes page (others)." | owner | **FIXED.** `AnnotationCard` has omitted the speaker whenever a chip draws it since the ladder landed — but only on the branch that BUILDS the meta line, and the Quotes screen passes an explicit `meta`, so it never reached that branch. And `omitSpeaker` now drops the NAMES rather than the line: it used to return the flat string, throwing away the leading mark, which is nobody's duplicate — on a quote with no speaker it is the only thing standing where every other card begins with a face |
| G11 | **The harness could not see any of this**, which is why G4, G9 and G10 all shipped. `seed.mjs` builds public-domain titles with **no cover artwork** (every image fetch in this container returns 403) and a cast of three, so no probe run against it can see a poster behind a glyph, a name long enough to truncate, or a card whose chip and credit line print one performer. | self, after the owner sent their own backup | **FIXED** — `run-with-backup.sh` boots a scratch server on a fresh data dir and restores a sealed archive through `POST /api/auth/restore/upload`. **Its first cut posted to `/auth/restore/upload`** — without the `/api` prefix every JSON route is mounted under — **and got 200 and the SPA shell**, which the script read as success: a run would have measured an empty library and called it clean. It asks the app now (`needs_onboarding` is true exactly while the users table is empty). CLAUDE.md carries the rule and `.gitignore` covers `*.tpbk`, so the archive can never be committed |
| G12 | **The probe signed in as the harness account against a restored library**, which has never heard of it — every surface would have reported "did not render", a run that measures the login screen twenty times. And `run-with-backup.sh` used `exec`, which replaces the shell so its EXIT trap never fires: four scratch servers and their mktemp data dirs — holding a restored library — were left behind before it was noticed. | self | **FIXED** — `--username`/`--password` and `TIPPANI_USER`/`TIPPANI_PASS`; the runner keeps its shell so the trap reaps the server and the directory |

## E. Duplication and documents

| # | Defect | Source | State |
|---|---|---|---|
| E1 | **`ui.jsx:4093-4105` duplicates `4202-4214` verbatim** and is absent from the duplication audit that was supposed to find exactly this. | rater | **FIXED — and the cited lines were wrong, the finding right.** 4093-4105 is a comment block and 4202-4214 a ConfirmDialog; they are not duplicates. A sweep for repeated 8-line blocks found three real ones: the scrim portal and card wrapper in `FormModal` and `HelpSheet`, the clamp chevron's Tooltip in `HandNote` and `ExpandableText`, and the whole matchMedia subscribe/sync/unsubscribe in `useIsMobileScreen` and `useHideOnScrollDown`. The scrim's class list was written out in full in **nine** places across seven files, and it is the class carrying `overscroll-behavior` — a copy that drifts scrolls the page behind the dialog. Now `SCRIM`, `SCRIM_CENTERED`, `backdropClose`, `ClampToggle` and `useMediaMatch`, with `scrim-dismiss.test.jsx` pressing the backdrop and the card |
| E2 | **`codebase-audit.md` §4.1 records the credit row as "Match"** when three of its details deviate (A4, A5, A11). | rater | **FIXED** — the row now records what it got wrong and why: the reading looked at what the row DREW and not at what its controls DID. Three other verdicts in the same table went stale as this session fixed them (portrait verbs, picture size, header) and are updated with the commits |
| E3 | **§4.2's artboards are still uncompared** — `char-global`, `char-book`, `char-game`, `people-global`, and `book-detail*.dc.html`. The Match Picker and Fetch Results have now been compared and match. | rater, self | **FIXED for the four character/people boards.** All four compared row by row in §4.2 — the pack builds each as an ordered `rows: []`, so it is a list against a list rather than an impression — and all four match, including `char-game` having no "Dubbed by" and `people-global` ending in a delete. Two departures survive with their arguments: `char-global`'s three (named in `identityGlobal.jsx`'s header before this was read) and the local boards' one insertion, the per-work description row. `book-detail*` is split out as **E8** rather than folded into a claim |
| E8 | **`book-detail.dc.html` and `book-detail-wide.dc.html` are uncompared** — 4,504 and 5,957 lines, the whole work-detail screen: hero, tabs, quote board, artwork and id strips. A different area from the character and people screens this register covers, and its own piece of work. | self | **PART DONE.** The two parts a reader actually looks at are compared row by row in §4.3 — the hero (nine elements, eight matching) and the quote card's tool row — with two departures, each argued in the code before this was read: the count in the hero (`work-hero.test.jsx`'s four questions) and copy split out of share (a hold-to-copy is a gesture a reader has to be told about). **Still uncompared, named rather than implied**: the sticky bar the board grows on scroll, the chapter grouping and its filter strip, the drawer, every dialog the board opens, and the whole wide twin |
| E4 | **§2.2 and §2.3's tautology sections remain unfixed**, which was half of the owner's sixth instruction. | rater | **FIXED** — all six of §2.2 and §2.3's hole. Each replacement is a render or a resolved value: `dom/key-legends.test.jsx`, `dom/hidden-section.test.jsx`, `dom/month-axis.test.jsx` (in Bengali, because in English a cut and the table both produce "Jan"), two cases in `dom/work-tile-marks.test.jsx`, an extended `scroll-containment.test.js`, and `test/css-cascade.js` — the resolver lifted out of `accent-texture.test.jsx` so the four byte-level matchers can ask for a value. Two live gaps fell out of it: the drawer's account row printed no key for `g p`, and E6 below |
| E5 | **The ✕ rule contradicts itself.** `PanelHost` reds it only when `dirty > 0`; `identityPicker.jsx` passes `closeDanger` unconditionally; CLAUDE.md states it flatly. One of the three has to give. | rater | **FIXED** — `PanelHost` gave. The `dirty` gate belongs to the TICK and only to it: the tick's arming answers "has something changed", the cross's colour answers "what does this press do", and asking both the same question signals one fact twice while leaving the second unanswered — and leaves a reader who has typed nothing unable to tell a form they may leave freely from a form at all. CLAUDE.md is amended to state the rule as it now holds; `cross-is-red.test.jsx` holds both surfaces side by side, and 3 of its 9 cases fail against the old gate |
| E7 | **Forty-three glyphs across seventeen files are typed characters, not the app's drawings.** `♥ ▲ ▼ ▾ ▸ → ↗ ✓ ✕ ⚠ ⌃ ⌄ ⇢ 🗑 ✎ ❞ ❑` — the sort arrows on three tables, the favourite mark on five screens, the arrow between an import's before and after, the tick in a menu row, the identity sheets' own row icons. CLAUDE.md's rule is flat and the repo had already fixed this exact class once, on the credit row, with nothing stopping the next one. | self | **FIXED** — all 43. Two new drawings were needed (`IconArrow`, `IconWarning`) and are documented in the glossary; `glyphs-are-drawn.test.js` is the ratchet, and three existing tests were keyed to the CHARACTER rather than to the feature and had to be retargeted — which is the same defect class in the tests |
| E6 | **The shared cascade resolver answered with an ANCESTOR's declaration.** `competes(sel, target)` reduced the CANDIDATE to its rightmost compound and then compared it against the whole TARGET, ancestors included — so `.drawer-item.active .review-dot`'s background resolved to `.drawer-item.active`'s fill. Every target it had been asked about was a single compound, where the two readings coincide. | self | **FIXED** — the target is reduced the same way as the candidate |

---

## Fixed, with the commit that did it

| # | Defect | Fixed in |
|---|---|---|
| F1 | The ⋯ menu opened an empty card on six of twelve screens | `a94e141` |
| F2 | The character sheet's rows scrolled to a fold instead of opening an editor | `a94e141` |
| F3 | "Called here" and "Also called here" were two rows editing one fact | `a94e141` |
| F4 | Three identity sheets committed with a body "Save" button, not the standing pair | `a94e141`, `ecd65d4` |
| F5 | `PanelHost`'s tick never armed and never counted; its ✕ was never red | `ecd65d4` |
| F6 | Every card's ⋯ lacked `aria-haspopup` and `aria-expanded` | `ecd65d4` |
| F7 | The rail brand and the root crumb could not say "you are already here" | `d0304f4` |
| F8 | "Add another performer" was a text box, not the library's people | `c6b1ae3` |
| F9 | A claim made in three documents that `PanelHost` already drew the armed tick | `ecd65d4` (corrected in both docs) |
| F10 | `0d84411`'s claim that three cast writers never set `character_id` | `a94e141` (reverted, corrected in the audit) |

## Withdrawn claims

Kept because the pattern matters more than any one of them.

- **"Dead controls went 44 → 19 → 1 → 0."** Desktop only. The phone pass was never run. See D2.
- **"Three of the four writers of `work_cast` never set `character_id`."** All four already
  called `LinkCastRow`. The mutation that seemed to prove otherwise was detecting damage
  the fix itself made possible.
- **"44px is stated in CLAUDE.md."** It is not; CLAUDE.md contains no "44". The number is
  the design pack's.
- **"`--type-ui-14` exists."** It is not on the scale, and `typescale.test.js` names it as
  its example typo. I had grepped for the token's USE and read my own new line back.
