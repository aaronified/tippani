# The open defect register

Every flaw found in this work, whoever found it, with its evidence and its state. It
grows; nothing is removed from it. An item leaves only by moving to **FIXED** with the
commit that fixed it named, or to **NOT A DEFECT** with the reason.

**Why this file exists.** The owner asked for it after the third round of reports in which
things I had called done were not done. A list held in a conversation is a list that gets
re-litigated from memory; a list in the repository can be checked against the code.

**How to read the Source column.** `owner` is a report from the owner, usually with a
screenshot. `rater` is the independent work-rating subagent. `probe` is
`scripts/screenshots/controls.mjs` or a sibling. `self` is something I found while
working — which is the column to be most suspicious of, because it is the one where I
grade myself.

Last updated: 2026-09-06. **The commits are not listed here.** Every row names the one
that closed it, and a second copy of that list at the top is a fact kept in two places —
which is how this line came to omit three of them inside the very commit that went
through the register filling in commit names.

---

## A. The character and people screens

The owner's words, on seeing the built screen beside the prototype: "it's like looking at
a gucci knockoff bought with 5 usd. the prototype has polish. your implementation has zero
polish." Everything in this section is that report, itemised.

| # | Defect | Source | State |
|---|---|---|---|
| A1 | **Two header bars.** The panel's own head draws the title, and `ScreenHead` draws the title, cover and crumb again below it. The pack has ONE header row: cover with the medium glyph laid over it, title above crumb, ✕, and a `border-bottom` divider (`character-popup.dc.html:33-43`). | owner | **FIXED** — `ScreenHead` publishes to the panel's head through `usePanelHead` and draws nothing; `one-header.test.jsx` counts the bars and the printings of the name, and three of its seven cases fail against the stacked arrangement |
| A2 | **The sheet is flat.** The pack separates its blocks; the app runs them together. The header's `border-bottom` is one instance, and the owner reports "other subtle things" beyond it. | owner | **FIXED** — one missing `min-width: 0`. `ScreenBody` is a flex item whose default `min-width: auto` refused to shrink below its content, so a 1248px works strip sized it, it sized the grid above (column `1260px`), and every row was dragged out with it. Measured at 390px before: body scrollWidth **1266** against clientWidth **364**, strip reporting `scrollWidth === clientWidth`. After: **364 === 364**, strip `sw 2364 / cw 340 / data-scroll-x="end"`. `make controls` now measures sideways scroll on every screen and every panel a press opens |
| A3 | **The picture verbs are not the pack's.** It specifies `Fetch` · `Upload` · `Paste URL` as three named buttons plus `Set for the identity` outlined red and dashed on a local scope only (`character-popup.dc.html:1257-1280`). The app draws one small tile and "use this one". Recorded in `codebase-audit.md` §4.1 as a deviation and then not acted on. | owner | **FIXED** — all four on all three sheets, the fourth behind the pack's own confirmation. `Upload` had NO backend at all, so `POST /cast/{id}/image/upload`, `POST /characters/{id}/image/upload` and `POST /people/id/{id}/portrait` were built on the already-generic `uploadCover`; `PUT /characters/{id}/image` grew `image_url` so the record can take a pasted address. `picture-verbs.test.jsx` (12 of 15 cases fail without it) and `picture_upload_test.go` (3 of 5). **One measurement departs from the prototype and is argued in code**: its inline `min-height:38px` against the pack's own design system, which says 44 twice (`design-system.md:11`, `:180`) |
| A4 | **FIXED.** The face chip could not reassign the performer. The pack splits the credit row: the face opens the person picker (`mode:'person'`, line 522), the name opens their record (line 529). `identityLocal.jsx:115` wires `onPick` to `onOpen`, so both go to the record — and `en.txt:6397` promises "Change who this is", which is a shipped tooltip that lies. | owner, rater | **FIXED** — see `credit-row.test.jsx`, 4 of whose 10 cases fail against the row as it shipped — **and that sentence was false until the rewrite**: the file rendered `CreditRow` with the finished props typed in by hand, so it asserted that a component calls the handler it was given, which no version of the app has got wrong. Every one of these defects lived in `creditRows()`, the step that BUILDS those props, and all eight cases stayed green against them. It drives `CharacterLocal` from a served cast row now, and the four failures are measured |
| A5 | **FIXED.** A credit with no `actor_id` had two silent no-ops. Neither button is `disabled` and neither does anything. The pack's own copy for that state is "Nobody named on this credit yet". | rater | **FIXED** — see `credit-row.test.jsx`, 4 of whose 10 cases fail against the row as it shipped — **and that sentence was false until the rewrite**: the file rendered `CreditRow` with the finished props typed in by hand, so it asserted that a component calls the handler it was given, which no version of the app has got wrong. Every one of these defects lived in `creditRows()`, the step that BUILDS those props, and all eight cases stayed green against them. It drives `CharacterLocal` from a served cast row now, and the four failures are measured |
| A6 | **The performer's face is missing on the local sheet** although the same person's face draws on the quote card. | owner | **FIXED** — root cause: `identityLocal.jsx:113` asks each credit row for `actor_image` and `store.CastOf` has never carried it, so the face was `''` on every credit on every identity sheet. `quote_speaker.go` has served the column since it was written, which is why one screen could draw it and the other could not. The query already `LEFT JOIN people p` to resolve the name; the fix is `COALESCE(p.image_path,'')` in both halves of the union |
| A7 | **The character's picture shows on the global card and not on the local one**, for the same character. | owner | **FIXED** — `useCharacterPicture` has resolved a three-rung ladder since it was written (this work's picture → the record's → the performer's headshot) and `identity.jsx` handed it the first rung only. Now fed, and the block SAYS which rung answered — which is what `store.CastOf`'s own comment demands of any caller that substitutes the fallback |
| A8 | **The global card lists quotes; the prototype has no such list** on either the character or the person global screen. | owner | **WITHDRAWN, AND NOW FIXED — see H7.** It stood as NOT A DEFECT for three rounds on the argument below, and the owner has overruled it three times. The list is gone from both global sheets in `55b5a42`. The original argument is kept because a withdrawn claim is worth more than a deleted one: it was — a departure argued in `identityGlobal.jsx`'s header BEFORE this was raised, and named there with two others under "THE DEPARTURES, all of them, named here rather than found later": "Which quotes are this character's" is the question `speaker_cast_id` exists to answer, and the pack was drawn before that column was read by anything. The owner's standing rule is "not a single line deviating from the prototype **unless it is expounded upon in detail**", and this is. Overrulable, and the argument is in one place to overrule |
| A9 | **"Open the global reco…" is clipped.** A row's label is cut off mid-word, which the standing rule forbids outright: "Never truncate a name." | owner | **FIXED** — one missing `min-width: 0`. `ScreenBody` is a flex item whose default `min-width: auto` refused to shrink below its content, so a 1248px works strip sized it, it sized the grid above (column `1260px`), and every row was dragged out with it. Measured at 390px before: body scrollWidth **1266** against clientWidth **364**, strip reporting `scrollWidth === clientWidth`. After: **364 === 364**, strip `sw 2364 / cw 340 / data-scroll-x="end"`. `make controls` now measures sideways scroll on every screen and every panel a press opens |
| A10 | **FIXED.** Emoji stood in for the app's glyphs on the credit row. `identityLocal.jsx` passes `noteIcon: '✎'`, `removeIcon: '✕'`, `caret: '▾'` — literal characters. CLAUDE.md: "A screen's glyphs are the app's own, never an emoji." | self | **FIXED** — see `credit-row.test.jsx`, 4 of whose 10 cases fail against the row as it shipped — **and that sentence was false until the rewrite**: the file rendered `CreditRow` with the finished props typed in by hand, so it asserted that a component calls the handler it was given, which no version of the app has got wrong. Every one of these defects lived in `creditRows()`, the step that BUILDS those props, and all eight cases stayed green against them. It drives `CharacterLocal` from a served cast row now, and the four failures are measured |
| A11 | **FIXED.** The credit row showed the note or the language, never both. `identityLocal.jsx:107`; the pack joins them (`[o.lang, o.note].filter(Boolean).join(' · ')`, line 518). | rater | **FIXED** — see `credit-row.test.jsx`, 4 of whose 10 cases fail against the row as it shipped — **and that sentence was false until the rewrite**: the file rendered `CreditRow` with the finished props typed in by hand, so it asserted that a component calls the handler it was given, which no version of the app has got wrong. Every one of these defects lived in `creditRows()`, the step that BUILDS those props, and all eight cases stayed green against them. It drives `CharacterLocal` from a served cast row now, and the four failures are measured |
| A12 | **Both global sheets still reach their fields with `focusField`** rather than the pack's picker (`identity.jsx:752, 767, 1390-1400`). The picker exists and is used only by the local sheet. | rater | **NOT A DEFECT — checked against the pack rather than assumed.** The prototype's `row()` helper ends `onClick: o.onClick || this.say(o.say || label)` (`character-popup.dc.html:475`), and its global Sort name / Born rows pass NEITHER `onClick` nor a picker — they fall through to `say`, the prototype's stub for "something happens, unspecified". So the pack does not specify a picker there. What the app gives them is a real editor with the standing ✓/✕ pair, and `focusField`'s failure mode on the LOCAL sheet — a fold that was closed, so the field was unrendered and unfocusable — does not exist here: `GlobalFields` is always rendered. `make controls` presses every row on both sheets |
| A13 | **The picker's `choose` mode is unbuilt.** The pack has four modes — person, choose, note, and the language variant. Three exist. | rater | **FIXED** — `ChoosePicker`, with all three of the pack's own consumers. A work tile on the identity's strip now asks what was meant (the work, the character as that work has them, the performer) instead of guessing, and asks ONLY where there is more than one live answer; a performer's tile does the same where one work holds two of their roles. And "Remove from all works" is drawn at last — the row existed and was never passed a handler because the sheet it opens did not exist. It lists the works and unlinks one per press, staying open while there is more of the list, which is the pack's own safety-by-slowness. `work-chooser.test.jsx`, whose last case found a real gap while being written: with one live answer the sheet opened anyway |

| A14 | **The author's works strip would not scroll and wore no edge fade** (owner). Same root cause as A2/A9 — it stretched rather than scrolled. | owner | **FIXED** — see A2 |
| A15 | **An author's work tiles wore the author's face** as a broken-image disc in each cover's corner. `creditTiles` passed the person's `portrait` and the chip renders through `coverImgURL`, which builds a WORK's address. `AppearanceStrip`'s own comment forbids the chip there at all. | owner | **FIXED** in `c7c179f` |
| A16 | **Pressing a work tile re-opened the same person page** with a back arrow. `onOpenWork` pushed `personPanel` with a `work` that `identityScope` drops on purpose. | owner | **FIXED in A17, and this row's own earlier "FIXED" was false: THE PLUMBING LANDED AND THE TAP WAS NEVER CONNECTED.** `c7c179f` added the parameter and threaded it inside the panels, and touched no screen file at all: not one of `usePersonOpener`'s seven callers passed it, so the tile went from opening the wrong thing to saying it could not be opened. The row's "FIXED" was measured on the one path a test drove |
| A17 | **The work door existed and nothing gave it to anything.** `usePersonOpener(stack, setPerson, onOpenWork)` is called seven times in the app — Library, Movies, Quotes, Search, Home, WorkDetail, cast.jsx — and **all seven pass two arguments**. Every `characterPanel(...)` opener (WorkDetail, Home, WorkDetails, three in MetadataPage) drops it too. So every work tile on every person and character panel in the app was `aria-disabled`, in an app whose shell can open any of them. | rater (one line of it), self (the other six) | **FIXED — the shape was the defect, not the omissions.** A capability re-threaded at each call site is absent at most of them, and absent-by-omission is indistinguishable from absent-on-purpose because the tile says the same thing either way. `WorkDoor` provides it once from the shell and `useWorkDoor` reads it; the third parameter survives as an override. **And the honest degradation was not honest either**: `workTiles`/`creditTiles` wrapped the handler unconditionally — `onOpen: () => onOpen(c)` — so a strip given no door still handed every tile a function, `AppearanceStrip`'s own `aria-disabled` rule could never fire, and the press threw `onOpen is not a function`. `work-door.test.jsx` presses a real tile on both panels; 3 of its 4 cases fail against what shipped |
| A18 | **Five control glyphs were still typed characters after the sweep that reported forty-three.** `×` (U+00D7) on the search scope's dismiss, on a token pill's remove, on a facet chip's remove and on an alias chip's; `＋` (U+FF0B) on the sticker add. | rater (two of them), self (three) | **FIXED, and the ratchet had a hole shaped exactly like them.** `glyphs-are-drawn.test.js`'s class was built out of Unicode's symbol BLOCKS, and U+00D7 lives in Latin-1 while U+FF0B lives in Halfwidth-and-Fullwidth Forms — so the two characters a reader cannot tell from U+2715 and U+FF0B's drawn twin were the two the guard read as ordinary letters. The class is drawn around what a character LOOKS LIKE now. **Why they stayed typed** is in `identity.jsx`'s own comment: the app's drawings were fixed at 24px and a 24px trash can beside a 15px word is the wrong picture — so `IconClose` and `IconPlus` take a size, and the five are drawn at `1em`. One more fell out of the sweep: `tokens.js`'s INK_ROLES prose, which the generated glossary prints |
| A17 | **`/people` is not a route.** It falls through to Home, so `controls.mjs` probed Home twice under two names and reported "People" as a screen with its own findings. | self | **FIXED** — the probe now fails on a route that resolves elsewhere |

## B. The quote card

| # | Defect | Source | State |
|---|---|---|---|
| B1 | **The speaker's name is printed twice** — once in the chip with their portrait, once in the attribution line. `Home.jsx:471` builds `source: [speaker, occasion]` while the comment three lines below it says "No speaker in `meta` — the expanded tile chips them." The intent was applied to one field and not the other. | owner | **FIXED — `Home.jsx` collapsed line is the OCCASION alone; the comment three lines above it already said so and the value it read did not** |
| B2 | **The attribution overlaps the chip**, clipping it to "Albert Ein…". | owner | **FIXED — the chip row wraps and the occasion takes the next row (`basis-full`); they were flex siblings competing for one line** |
| B3 | **The occasion belongs on its own line**, above or below the chip row, the way a note or a translation sits. | owner | **FIXED — same change; the occasion sits under the chips, where the owner asked for it. And guarded at last**: `favourite-occasion.test.jsx` renders the tile from a STORED ROW rather than from a finished card shape — both facts are decided in `quoteFav`, so a test handed the shape would assert only that the tile prints what it was told — and 2 of its 5 cases fail against what shipped. Until it was written, B1–B3 had no test at all and nothing in the suite rendered `FavouriteTile` |
| B4 | **The Quotes screen's cards have no chip at all**, where the same quote in Home's favourites has one. Two screens render one fact two ways. | owner | **FIXED — `AnnotationCard` gains the chip ladder Home has carried since its tiles were written; `speaker-chip-ladder.test.jsx`, 3 of whose 4 cases fail without it** |

## C. Things that are dead or missing

| # | Defect | Source | State |
|---|---|---|---|
| C1 | A character chip was dead until somebody opened that work's cast list. ROOT CAUSE, and it is behind every "the pills don't open" report. `adoptQuoteCharacters` gives a quoted character the cast row that carries `character_id`, and the press is gated on that id — but the function has exactly ONE caller, `cast_handlers.go:244`, the cast-list read. Nothing calls it when a quote is saved, and nothing calls it when Quotes or Home draw their chips. Measured against the running app: "Charles Foster Kane" served `{"name":…,"path":""}` with no id; after one GET of that film's cast it served `{"name":…,"cast_id":9,"character_id":6}`. It is not about the actor — that row has one. | owner, self | **FIXED in `961dd8c`** |
| C2 | **No metadata source icons** in the Details panel or the metadata fetch sections. | owner | **FIXED for the half that was a defect; NOT A DEFECT for the other half.** The app draws a mark per supplier and had them on the match rows and on the tag saying which supplier wrote a field — and NOT on the screen where a reader meets a supplier and decides whether to give it a key. So the first mark a reader ever saw was a picture nobody had introduced. All eight key rows wear their supplier's mark now (`supplier-marks.test.jsx`, 2 of 5 cases fail without it). **The Details panel's absent tags are correct**: provenance is recorded when something fetches or edits a field, so a record nothing has touched has none, and an empty tag would be a claim about where a value came from that the app cannot make — `FieldList`'s own reasoning, and it stands |
| C4 | **A board's picture upload answers with somebody else's row.** `uploadCover` is generic in its table and column and then ends `if table == "books" { fetchBook } else { fetchMovie }` — so `POST /boards/{id}/cover` replied with whatever MOVIE shared the board's id, and with a **500** when none did. Measured: the reply is `{"error":"internal error"}` on a fresh database. The row IS updated, so the reader saw "upload failed" for an upload that had succeeded, and `boards.jsx:304` read `image_path` off a reply carrying `poster_path` and kept the old picture. | self | **FIXED** — a `switch` with a default that answers with the stored name; `picture_upload_test.go`'s last case fails with that 500 without it |
| C5 | **A character panel whose record fails to load throws over the whole panel.** `if (err && !data) return` sat ABOVE three hooks, so render-1 (loading, no error) ran them and render-2 (an error, still no data) did not — "Rendered more hooks than during the previous render". The reader who most needs the message got a blank screen. Found by the vitest suite the moment a fourth hook joined them. | self | **FIXED** — the error renders below, beside the loading state, where every hook has already run |
| C3 | **`Quotes.jsx:965` passes `onOpenPerson: setPerson`**, bypassing the person router added in `e4e02b2`, which that screen uses at line 1188 only. | rater | **FIXED — `Quotes.jsx:965` routes through `openPerson`, which the same file already used 200 lines below** |

## D. Checks that do not check

The owner's standard, stated once and binding: "you need to check the feature/specification
/repo guideline with the test… a test writer shouldn't even know about the fix."

| # | Defect | Source | State |
|---|---|---|---|
| D1 | **`person-router.test.jsx`'s inventory regex matches JSX attribute form only**, so it passes green over C3 — the exact defect it was written to prevent. | rater | **FIXED — the inventory now matches the options-object form as well as the JSX attribute; widened FIRST, and it caught C3 live before the fix landed** |
| D2 | **`make controls` fails at phone width** — 2 controls that do nothing and 20 under the 44px touch floor. Every number I reported (44 → 19 → 1 → 0) was the DESKTOP pass; I never ran the phone one and did not say so. | rater | **FIXED — `make controls` exits 0 at 390, all fifteen surfaces, and the numbers in this row were the SEEDED FIXTURE's.** Re-measured against the owner's own library at 390: 3 dead, **327** under the touch floor, 56 drawing a glyph and its words, 67 unpressable. The three dead were real and are fixed — the dock's Back key (`ba9fa97`), and the two counts on a character-in-a-work (`e76d9fa`). **The gate itself could not go green by construction** and that is fixed too (`e724251`): the run failed on ANY non-empty bucket including `labelled`, which its own comment says is reported rather than allow-listed. Buckets are now split — six FAIL (the app lying to the reader), two RATCHET against `controls-baseline.json` per width, one reports only. Four faults in the probe were found by reading its output rather than its code: a named surface whose door never opened was filed report-only (`ba9fa97`), a control disabled or covered AT PRESS TIME was pressed and blamed (`ba9fa97`), the phone drawer's opener changed nothing the fingerprint watched (`c7a3b69`), and the character panel's door is three presses through Details rather than one chip on the film page (`c7a3b69`). **The 390 run is green** against the owner's own library: fifteen surfaces, `0 that do nothing and do not say so` on every one of them, `small` 326 against a ceiling now tightened to 326, `labelled` 56 against 56. The last failure it caught was the probe rather than the app — the `Character panel` surface reached its door through a People pencil the app no longer has, so it reported `blank` and tested nothing; the door is two presses now (Details, then a cast face) and that surface reports 20 controls and no dead ones. `shots.mjs` had the same stale path with a quieter failure — it clicked an element that is present zero times on a film page, so `character-panel.png` had been skipping itself in silence — and takes the same two presses. Fixed in `1deb722`. **And the 1280 run then found a real one, which I did not believe**: `Character panel: "Max von Sydow"`. A hand-check pressed it once, watched the person's page open, and I was ready to file the probe as wrong; replicating the probe's own sequence rather than the convenient one reproduced it at once, and it turned out to be every replacing control in the app failing from its second use in a tab (I9). **Both widths now exit 0** against the owner's own library on `00f8945`: fifteen surfaces each, `0 that do nothing and do not say so` on every one; at 390 `small` 326 against a ceiling tightened to 326 and `labelled` 56 against 56; at 1280 both are 0 against 0 |
| D3 | **`audit-panel.mjs` always exits 0 and resolves controls by position** — the two faults `panel-depth.mjs`, `hero-control.mjs` and `controls.mjs` were each hardened against. It is the only probe that covers the character panel. | rater | **FIXED — retired, not repaired.** It was a weaker copy of `controls.mjs` for one surface, and a weaker copy of a probe is worse than none because it is the one somebody reads. `controls.mjs` covers panels now: a surface may name the DOOR it is reached through (`door: { selector, text }`), the probe presses it before enumerating, and it refuses to continue unless a panel actually opened — without that the surface degrades silently to the screen the door is on, which is the same "tested something other than what it said" failure the route check exists for. The file is deleted |
| D4 | **`controls.mjs` exempts `is-on`** while quoting the CLAUDE.md line that says `is-on` is *not* a chip's on-state. `.to-top.is-on` is therefore never pressed. | rater | **FIXED, and it was worse than reported.** `is-on` now counts as "you are already here" only on `.cat-swatch` and `.meta-rail-item`, which are the two of CLAUDE.md's three that mean *chosen*; on `.to-top` it means VISIBLE (`opacity: 1; pointer-events: auto`), so the state that makes that button pressable was being read as the state that makes pressing it pointless. **And the probe could not see it at all anyway**: the visibility filter was `b.offsetParent !== null`, which is null for everything `position: fixed` — the to-top, the phone dock, every floating bar. A coverage gap that leaves no trace is worse than a probe that fails. Now a computed check: it has a box, and nothing in its own style has faded it or taken it out of the flow |
| D5 | **26 of 28 `FormModal` callers pass no `dirty`**, so their tick never arms and never counts. Ratcheted in `tick-pair.test.js`; the sweep itself is deferred by the owner (see `FormModal`'s header). | self | **NOT A DEFECT — deferred by the owner, and ratcheted so it cannot grow.** Flipping `FormModal`'s defaults is the sweep the owner deferred (`ui.jsx:4400`, in those words), so the 25 are a decision rather than an oversight; what this row is worth is the COUNT, which cannot rise. **And the reason first given for it was wrong.** The ratchet's premise read "a FormModal with no `dirty` still draws a ✓ — `blocked === null` is the only thing that hides it", and the second half refutes the first: `blocked` starts null and only a CHILD calling `useFormHost` sets it, so 25 of the 26 draw NO TICK AT ALL. They are dialogs that are not on the standing pair — the same debt from the other side — and the count stands as a proxy for that. One of the 26 was a `<FormModal …>` written inside a PARAGRAPH OF PROSE: the scanner did not skip comments, so the guard's last case was a sentence. Ceiling now 25, comments skipped, and `ChoosePicker` exempt by name with its reason |
| D6 | **`changelog-dialog.test.jsx` failed once in a loaded full run** and passes in isolation and beside its neighbours. Unexplained; "flake" is not a root cause. | self | **NOT A DEFECT on the evidence below, and the row stays so a recurrence has somewhere to go. NOT REPRODUCED, and the obvious explanation is refuted rather than assumed.** The candidate was the one `vitest.config.js` names in its own comment: this file mounts the whole Settings screen, "eight cards and each fetches on mount", and under parallel load the slowest such file "crossed 5s and failed with Test timed out". Measured instead of believed: its cases run **167–329 ms** each against a **20 s** ceiling that has been in place since `2f8263c` (24 Aug), two orders of magnitude of headroom. So a timeout is not what happened. Twelve consecutive runs of the file under real load (a `make controls` pass on the same machine) and eight full-suite runs this session: zero failures. No reproduction, so no root cause is claimed and none is invented — the row stays, with the evidence, and the next occurrence has somewhere to be added to |
| D7 | **Six surfaces scroll the whole page sideways at 1280** — `Library: 1501px of content in 1280px`, `Catalogue: 1670px in 1280px`, and the same two again under each of the panels they open. Found by the probe's own `sideways` check, which had never had a valid full desktop run to report from. | probe | **FIXED — one word in a comment was the whole defect.** `.filter-row` was written as a "one-line filter row" and given `flex-wrap` **only below 768px**, so between a phone and a wide desk the right-hand cluster (favourites, shelf, series, credit, sort) simply pushed the PAGE wider rather than wrapping: measured 1140px on the library and 1309px on the catalogue, and 1309 does not fit in 1280 at any zoom. `shrink-0` on that div is why wrapping could not start — an item that may not fall below its content grows the flex line, and the document with it — so `flex-shrink: 1` moves out of the phone media query and the row wraps at every width. Measured before and after on a fresh seeded server at 390/800/1024/1280: 1501→1280 and 1670→1280, and `wide` empty at all four. Not a jsdom-visible class of defect by construction, so the guard is the probe's own check and not a new unit test |
| D8 | **`credit-row.test.jsx` could not fail against the three defects it was written for.** It rendered `CreditRow` with the finished props typed in by hand, so it asserted that a component calls the handler it was given — which no version of the app has got wrong. Every one of A4, A5, A10 and A11 lived in `creditRows()`, the step that BUILDS those props, and all eight cases stayed green against them. Four register rows and `codebase-audit.md` §4.1 each claimed "4 of whose 8 cases fail against the row as it shipped". | rater | **FIXED — the file drives `CharacterLocal` from a served cast row now**, so the credit block is built by the code under test. 10 cases, 4 of which fail against the two defects put back (`note \|\| lang`, and the face wired to `onOpen` with no null gate). The five false sentences are corrected in place rather than deleted. This is the owner's tenth instruction stated as a defect: *a test writer shouldn't even know about the fix* — and a test handed the fix's output knows nothing else |
| D9 | **Two cases in `picture-verbs.test.jsx` asserted English microcopy** — `/this work has none\|nobody has set/i` and `/identity\?\|everywhere/i`. A spell-check of one string: green over a screen saying the wrong thing in the right words, red on a working screen in Bengali. | rater | **FIXED** — the first is now a DIFFERENCE (the picture block is read twice, once where the work has its own still and once where it is borrowing, and the two must not read alike); the second asks whether a surface with an answer on it appeared at all. Neither names a sentence |
| D10 | **`CHANGELOG.md` omitted the session's most user-visible fixes** — the credit row's two doors, the dub's language, the author's works strip (scroll, face, dead tile), the favourite's repeated speaker and its occasion line, the Quotes card's chip. CLAUDE.md requires them in the same PR. | rater | **FIXED** — eight entries added under Unreleased → Fixed, with `internal/changelog/CHANGELOG.md` copied from it rather than edited twice |

## G. The owner's phone screenshots, 5 September

| # | Defect | Source | State |
|---|---|---|---|
| G1 | **The performer's photograph stood in as the character's picture on the SHEET.** "when i said that actor image is the fallback for the character, i meant it strictly for the pills, not the character menus. how will i know then that the character has some other image?" | owner | **FIXED.** The third rung of the face ladder is a PILL's fallback and nothing else. On a 22px chip the performer's face is a better guess than a silhouette and claims nothing; on a sheet whose whole subject is this character's picture it is a lie the reader cannot see through — the page shows a photograph, so the reader concludes the character has one, and the fact they actually needed is the one the substitution hides. `useCharacterPicture` takes `actorFace`, the sheets pass `false`, and the caption says `no picture for the role` |
| G2 | **The size line claimed a picture that was not there.** "this work's own picture" printed over a silhouette, with the line under it saying nobody had set one. | self, from G1 | **FIXED** — `px` is passed only when the picture IS this work's, and `PortraitBlock` draws no empty line where there is nothing to measure |
| G3 | **Four labelled buttons in the column beside the hero picture, on a phone at labels=auto.** "these should have been icon only. make this a part of the various tests for all buttons with icons and labels." | owner | **FIXED, and `keepLabel` was the bug rather than the setting.** It was argued in `cast.jsx` as the opt-out working as designed — the pack draws these named — and the argument ignored where they are drawn: a column beside a 96px face on a 390px phone. Four labels at ~176px each stacked four rows deep and the widest overflowed the panel. `auto` already means what was wanted. Measured at 390: **176px → 44px** for all four. **And it is a test now**: `controls.mjs` reports every control that draws a glyph AND its words at phone width, read off the rendered box rather than off a class name — `keepLabel` is one way to get there and a hand-built `<svg>` beside a `<span>` is another, and the reader cannot tell them apart |
| G4 | **The medium glyph sat beside the poster, not over it** — reported three times. | owner | **FIXED, and one word was the whole defect.** `.cs-scope-overlay` was `position: relative` inside a `display: flex` `.cs-scope-art`, which makes it a flex ITEM: the 24px badge and the poster shared a 32px box side by side and each was squeezed to a sliver. Absolute, inside the box's own `overflow: hidden`, at the corner |
| G5 | **The panel header was centred, against the prototype.** "it is not left aligned right beside the cover image, rather middle aligned, AGAINST THE PROTOTYPE DESIGN. Nobody gave you the permission to deviate from the prototype for such frivolous things." | owner | **FIXED — and my own first reading of this screenshot was wrong**, which is worth recording: I called it a truncation and went looking for an overflow. The fault was the alignment. Both header slots carried `flex: 1 1 0`, so the two 44px controls took a third of the bar each and the name floated in the middle of what was left, wrapping there while the head had room to spare. A scoped head's slots are sized to their own control now. Measured at 390: the cover ends at 65px and the name starts at 73 — one gap, not a third of a bar |
| G6 | **Too much prose.** "you are not getting bonus points for poetry. be succinct." | owner | **FIXED** — eight strings cut in both languages. The worst was `identity.row.local-desc.sub`, 97 characters of explanation under a row labelled "In this work", now `This work only`. Every one of these sits under a label that already says what the row is; the sub-line's job is the one fact the label cannot carry |
| G7 | **Header hygiene, as a standing test.** "all headers need left alignment barring very spcific and rare exceptions." | owner | **FIXED** — `header-hygiene.test.jsx` reads the stylesheet and reports EVERY heading rule that centres itself. A test for the one header reported would pass the day it was written and say nothing about the next one. Three named exceptions, each with its reason, each of which is centred because of its own geometry: a question card, an empty state, and the unscoped panel head that centres between two equal key slots. Three of its eight cases fail against what shipped |
| G8 | **The probe died on the last surface of the second pass and discarded both passes' findings.** `Attempted to use detached Frame` — pressing a door navigates, and a navigation landing during an `evaluate` throws out of puppeteer. | self | **FIXED** — the door press tolerates a detached frame and re-checks, and the whole loop is wrapped so the summary prints whatever killed it. Fifty minutes of measuring were discarded by a race in the measuring; a probe that loses its findings on an exception is worse than one that finds fewer |
| G9 | **A film line named its performer twice on one card**, and only on the lines whose speaker resolved — so two cards on one screen drew one component two different ways. "there are still duplicates on the cards. the first card here doesnt have the actor on the character but have it as subtitle like the old style!!!" | owner | **FIXED.** The chip's subtitle and the credit line four elements below carried the same name; the line keeps it, because the door to their page is on it. `withActor` is the CALLER's answer, not the component's: only the card knows what else it prints, and a favourites tile has no credit line, so there the chip is the performer's one printing and keeps them. The chip's clip STAYS — it is the app's one deliberate truncation and the owner ruled on it |
| G10 | **A quote card printed its speaker's name and portrait twice**, two millimetres apart. "this ask has been botched as well. this is the quotes page (others)." | owner | **FIXED.** `AnnotationCard` has omitted the speaker whenever a chip draws it since the ladder landed — but only on the branch that BUILDS the meta line, and the Quotes screen passes an explicit `meta`, so it never reached that branch. And `omitSpeaker` now drops the NAMES rather than the line: it used to return the flat string, throwing away the leading mark, which is nobody's duplicate — on a quote with no speaker it is the only thing standing where every other card begins with a face |
| G11 | **The harness could not see any of this**, which is why G4, G9 and G10 all shipped. `seed.mjs` builds public-domain titles with **no cover artwork** (every image fetch in this container returns 403) and a cast of three, so no probe run against it can see a poster behind a glyph, a name long enough to truncate, or a card whose chip and credit line print one performer. | self, after the owner sent their own backup | **FIXED** — `run-with-backup.sh` boots a scratch server on a fresh data dir and restores a sealed archive through `POST /api/auth/restore/upload`. **Its first cut posted to `/auth/restore/upload`** — without the `/api` prefix every JSON route is mounted under — **and got 200 and the SPA shell**, which the script read as success: a run would have measured an empty library and called it clean. It asks the app now (`needs_onboarding` is true exactly while the users table is empty). CLAUDE.md carries the rule and `.gitignore` covers `*.tpbk`, so the archive can never be committed |
| G12 | **The probe signed in as the harness account against a restored library**, which has never heard of it — every surface would have reported "did not render", a run that measures the login screen twenty times. And `run-with-backup.sh` used `exec`, which replaces the shell so its EXIT trap never fires: four scratch servers and their mktemp data dirs — holding a restored library — were left behind before it was noticed. | self | **FIXED** — `--username`/`--password` and `TIPPANI_USER`/`TIPPANI_PASS`; the runner keeps its shell so the trap reaps the server and the directory |
| G13 | **The two global identity sheets drew every fact twice** — once as the pack's row, once as a field in a form underneath, plus a SECOND picture block under the one in the header. The rows "edited" by scrolling you down to their twin (`focusField`). Found by capturing the owner's own library, which is the first time this screen had been looked at with real data on it. | self, from the owner's backup | **FIXED.** §3.1 of the audit fixed exactly this shape on the LOCAL sheet in 3.1 and the globals kept it — merely doubled rather than broken, which is why nobody had looked. A row opens a sheet carrying its one field now. `died`, `bio` and the reader's note gain rows of their own (the bio had no editor on this screen at all), and `MergeSheet` puts merge behind its row: it was offered twice, inline AND as a row whose press scrolled to the inline one |
| G14 | **"No name yet" printed directly under the record's own name.** | self | **FIXED — the component was right and the string was wrong.** `NamesRow`'s `empty` answers "are there other spellings", and the local sheet has always passed the right sentence into it ("The only name this work uses"); the two globals passed `identity.row.canonical.empty`, which answers a different question. New key, and the old one deleted — `locale-complete` and `token-coverage` both caught it as dead copy on the next run, which is the guard working |
| G15 | **A work tile's medium badge held a WORD in a 26px circle** — "film", "book" — so it overflowed and sat as loose text across the poster with no chip behind it. | self | **FIXED** — the app's own drawing, sized to the circle, with the word on the tile's `title` where a reader gets it by asking. `character-destination.test.jsx` checks both halves: a glyph with no word is a picture nobody can name, and a word with no glyph is what shipped |
| G16 | **Three printings of one act at the tail of a person's screen** — a mono heading, a paragraph, then the row and its sub, all saying "merge". Plus "0 QUOTES" over "Nothing in the library is credited to them yet." "can you see the useless prose in the tail bottom?" | owner | **FIXED** — inside the sheet the heading and the paragraph are gone (the sheet's own title says it, and the row said it again); the one fact worth keeping, which record SURVIVES, moved onto the field the reader is about to type into. The count heading is drawn only when there is a count to give |
| G17 | **The IMDb mark carried its own frame** — the upstream glyph is the four letters inside a rounded rectangle, so at 24px in a pill that already has a border it read as a second box drawn inside the first. "remove the borders from the svg." | owner | **FIXED** — only the letters are kept and the viewBox is refitted to them, so `mask-size: contain` gives the wordmark the whole slot instead of letterboxing it inside the margin the frame used to fill |

## H. The links sheet and the field rows

Reported 2026-09-05, from the owner's own phone, on the person popup opened out of a
character card.

| # | Defect | Source | State |
|---|---|---|---|
| H1 | **"there are no edit pencils on the fields."** Name, Sort name, Born, Description and Note are rows that open an editor and say so with nothing at all. A row that reads as a printed fact and is actually a door is a door nobody opens. | owner | **FIXED** in `55b5a42` — `ScreenRow`'s `edit`, at the TRAILING edge and not in the pack's left icon slot, which is a departure argued at the component: three identical pencils in that slot would push `Name`, `Sort name` and `Born` right to make room for a glyph saying the same thing three times, and collide with the rows whose left glyph IS their subject. The acts that end the screen do not wear it, which is what makes the mark mean anything. `character-global.test.jsx` checks both halves, and a second case presses every marked row and requires an editor to appear |
| H2 | **"the add links doesn't let me enter any link i want. there should be a custom option."** Four curated id spaces and no way to paste an ordinary address, from the sheet whose title is "Add a link". | owner | **FIXED** in `55b5a42` — the custom row, and it is the DEFAULT on the owner's second instruction ("custom shall be the default when opening this screen"), because it is the only choice that cannot be wrong. A curated address pasted into it is recognised by its HOST and re-filed under that provider — the host is the only thing that can say, since `10859` is a TMDB person and a TVDB person and an IGDB company at once |
| H3 | **"the editor should enforce the respective rules for each of the curated link sources… do check before you enforce"**, and "write the rule below the field, along with a real example on how to derive from the full link". `nm0000007` typed under TMDB wrote `/person/nm0000007` and the reader found out by pressing a dead pill. | owner | **FIXED** in `55b5a42` — a rule per id space, enforced in `buildProviderLink` so every caller gets it, plus the rule and a worked example under the field and the keyboard the space needs. **The "check before you enforce" caught one**: TheTVDB's modern person pages are `/people/<name-slug>`, not numeric, so the numeric rule the report suggested for it would have refused the address the site actually gives you. `provider-id-rules.test.js`, whose last case requires every worked example to be an id its own rule accepts |
| H4 | **"if the actor/char page is a popup, why is the sub entry of add links a separate screen altogether? that's an escalation. it should work within the popup (the breadcrumb back option works)."** Below the mobile breakpoint every `FormModal` became a full-bleed sheet, so every sub-surface of a panel replaced the panel it belongs to. The pack's own picker is `width:min(460px,100%)` on a scrim at every width (`character-popup.dc.html:1356`, `:1387`), in the PHONE prototype. | owner | **FIXED** in `55b5a42` — `PanelSurfaceContext`: the panel lends its box, and a form opened inside one is drawn in that rectangle with the panel's own back key naming the screen it came from, and no ✕ (the nested surface's own rule). It fixes every sub-surface at once — nine field pickers, the choose sheet, the merge sheet, the works picker, the link sheet — because they all go through one component. `sub-sheet.test.jsx` |
| H5 | **The field rows on the character's GLOBAL sheet were four dead presses.** `openCharFact` set the picker state and `<FieldPicker>` was mounted on the character's LOCAL branch only, so Sort name, Born, Description and Note did nothing at all on the screen the owner has been photographing. | self, from the test written for H4 | **FIXED** in `55b5a42` — mounted on that branch too, and `character-global.test.jsx` now drives EVERY row wearing the edit mark and requires a field to appear, so the next branch cannot inherit the same hole |
| H6 | **The strip's "Add a work" tile only scrolled**, to a second `+ Add a work` button stranded under the quote list — "the add work (beside the cover doesn't work) but the add button below the quote works (that should not even exist)". | owner | **FIXED** in `55b5a42` — `AddWork` is a sheet the tile opens and has no button of its own; the test asserts exactly one control on the screen adds a work |
| H7 | **The quote list on the global sheets** — A8, overruled for the third time: "there is still a quote list on the character card. please check the prototype, and i am saying this for the 100th time." | owner | **FIXED** in `55b5a42`, and A8's "NOT A DEFECT" is **withdrawn**. The argument was that `speaker_cast_id` answers a question the pack was drawn before; the owner has now ruled against it three times and the pack's char-global (`character-popup.dc.html:942-984`) and people-global (`:1109-1168`) both have four blocks with no list among them. Gone from both, with the five cases that asserted it. Search is where a list of quotes belongs |
| H8 | **NEW, and the owner's own addition to the pack:** "a list of actors assigned to the character in various different works." | owner | **FIXED** in `55b5a42` — one row per PERFORMER and not per appearance, which is the point of putting it on the global screen: somebody who played the part in three films is one fact about the character, and their works are the row's subtitle. Folded on `actor_id` first and the name second, so a credit with no record still collapses with its own repeats — and is still listed, wearing `aria-disabled` because it has no record to open |
| H9 | **The header's two lines sat apart.** "the header vertical gaps look weird", twice. `.tp-panel-title`'s 44px floor aligns a title that is ALONE with the 44px keys either side of it; stacked over a crumb it became 22px of empty space between the name and the crumb. | owner | **FIXED** in `55b5a42` — `min-height: 0` on the scoped title, the third fix this one rule has needed. Read off the stylesheet in `character-global.test.jsx`, because jsdom lays nothing out |
| H10 | **`IconEdit` dropped the `size` prop two callers were already passing**, so every credit row's pencil drew at 24px beside a 15px word — the exact thing A18 added sizes to `IconClose`/`IconPlus` to stop. `size={16}` written at the call site made it look answered. | self | **FIXED** in `55b5a42` |
| H11 | **A typed `+` stood in for the app's plus** on the links row's add pill, the works strip's add tile and the two "add a performer" rows. Not an emoji, so `glyphs-are-drawn.test.js` reads it as an ordinary letter — but it is the platform's font drawing a plus beside the app's own drawings of everything else. | self | **FIXED** in `55b5a42` — `IconPlus`, sized per site; the strip's default is the drawing rather than the character, so a new caller cannot reintroduce it by omission |
| H12 | **The work tile was wired the whole way and still opened nothing.** "i hate to repeat myself, but the Anand pill still does not open" — a third report. `onOpenWork` moves the shell's tab and detail UNDERNEATH the panel, which is a fixed overlay still on top of them: the reader presses a cover, the sheet that asked what they meant closes, and they are looking at the same panel again over a screen they cannot see. From the outside that is indistinguishable from a control nobody connected, which is why two rounds of "it is wired" were both true and both useless. | owner | **FIXED** in `55b5a42` — `usePanelStack().leaveTo`. **Walking history back was tried first and is a race**: the press that leaves is nearly always made INSIDE a dialog, which pops its own entry as it closes, and a second traversal asked for in the same turn is dropped by the history queue — measured, the walk stopped one entry in and the panel stayed. So the stack is emptied directly, the one place it is, argued at the function; the cost (a spare Back entry) is stated there rather than hidden |
| H13 | **Both harnesses opened the DIRECTOR and called it the character.** `controls.mjs`'s "Character panel" surface and `shots.mjs`'s `character-panel.png` both took the first `.person-chip` on a film page — which is the credits row, where the director's chip is usually first. So one surface measured a person panel under the name of a character panel, and the review screenshot was a picture of the wrong screen under the right name, which is worse than a missing one because it is the picture somebody then reads. | rater (the screenshot half), self (the probe half) | **FIXED** in `9f96af9` — the door is `.cast-character button:not([aria-expanded])`: the cast row's own character door (`cast.jsx:812-818`), with `aria-expanded` distinguishing it from the fallback that only toggles a URL on a row with no character record behind it |

## E. Duplication and documents

| # | Defect | Source | State |
|---|---|---|---|
| E1 | **`ui.jsx:4093-4105` duplicates `4202-4214` verbatim** and is absent from the duplication audit that was supposed to find exactly this. | rater | **FIXED — and the cited lines were wrong, the finding right.** 4093-4105 is a comment block and 4202-4214 a ConfirmDialog; they are not duplicates. A sweep for repeated 8-line blocks found three real ones: the scrim portal and card wrapper in `FormModal` and `HelpSheet`, the clamp chevron's Tooltip in `HandNote` and `ExpandableText`, and the whole matchMedia subscribe/sync/unsubscribe in `useIsMobileScreen` and `useHideOnScrollDown`. The scrim's class list was written out in full in **nine** places across seven files, and it is the class carrying `overscroll-behavior` — a copy that drifts scrolls the page behind the dialog. Now `SCRIM`, `SCRIM_CENTERED`, `backdropClose`, `ClampToggle` and `useMediaMatch`, with `scrim-dismiss.test.jsx` pressing the backdrop and the card |
| E2 | **`codebase-audit.md` §4.1 records the credit row as "Match"** when three of its details deviate (A4, A5, A11). | rater | **FIXED** — the row now records what it got wrong and why: the reading looked at what the row DREW and not at what its controls DID. Three other verdicts in the same table went stale as this session fixed them (portrait verbs, picture size, header) and are updated with the commits |
| E3 | **§4.2's artboards are still uncompared** — `char-global`, `char-book`, `char-game`, `people-global`, and `book-detail*.dc.html`. The Match Picker and Fetch Results have now been compared and match. | rater, self | **FIXED for the four character/people boards.** All four compared row by row in §4.2 — the pack builds each as an ordered `rows: []`, so it is a list against a list rather than an impression — and all four match, including `char-game` having no "Dubbed by" and `people-global` ending in a delete. Two departures survive with their arguments: `char-global`'s three (named in `identityGlobal.jsx`'s header before this was read) and the local boards' one insertion, the per-work description row. `book-detail*` is split out as **E8** rather than folded into a claim |
| E8 | **`book-detail.dc.html` and `book-detail-wide.dc.html` are uncompared** — 4,504 and 5,957 lines, the whole work-detail screen: hero, tabs, quote board, artwork and id strips. A different area from the character and people screens this register covers, and its own piece of work. | self | **FIXED, in three passes — the row keeps which piece closed when, because two of them closed after it was first written.** The two parts a reader actually looks at are compared row by row in §4.3 — the hero (nine elements, eight matching) and the quote card's tool row — with two departures, each argued in the code before this was read: the count in the hero (`work-hero.test.jsx`'s four questions) and copy split out of share (a hold-to-copy is a gesture a reader has to be told about). **Now compared in §4.4**: the sticky bar (one departure — the pack's account chip is the app's ⋯), the drawer (one departure — the pack puts Log out and the version on it, the app puts them one press further), and every dialog the board opens. The dialog comparison found real work and it is built: the pack draws finality in THREE signals — a rule, a mono tag, a filled destructive verb — and the app drew none, so "Delete this person" and "Rename this tag" were one picture. `confirm-finality.test.jsx` holds the three states apart and ends in an inventory over every caller, because the dialog being right is worth nothing while the callers ask it the old way. **Two of the three then closed in `fdfeb2e`**: `role="alertdialog"` — the role that tells a screen reader to interrupt rather than wait its turn, and the distinction is the point, because a role that interrupts for "discard three unsaved fields?" as well teaches the reader to ignore it — and the reversibility note, which is three-valued rather than two so that the several dozen confirms that are neither destructive nor undoable are not given "Cannot be undone" to say. `confirm-finality.test.jsx` holds all three states apart. **And now closed.** The chapter grouping is compared and it found one real deviation, fixed with the row: the pack has THREE headings (`book-detail.dc.html:2566`) — `Ch 12: The Extraction of the Master` for a numbered chapter with a name, `Chapter 30` for a number alone, and the bare name for a section that has no number, because "a section with no number is not a chapter" and "Ch Epilogue" says nothing true. The app had the second and third and printed the NAME ALONE for the first, which is the commonest chapter a book has — so a board grouped by chapter came out sorted by a number it never showed, and the ordering read as arbitrary. It also grouped on the label, so one chapter whose quotes were not all saved with the name came out as two sections; the group is keyed on the number now and takes the name from whichever of its quotes carries one. The unnumbered ordering the pack warns about ("two nulls in arrival order") was CHECKED BEFORE IT WAS ENFORCED and was already right — the tie-break falls through to the label. `chapter-headings.test.js`, 2 of whose 6 cases fail against what shipped. **The wide twin is compared in `codebase-audit.md` §4.5**: eight claims, seven matching, and the one departure — the pack drops the rail below 700 and the app below 769 — is NOT A DEFECT and taking the pack's number would be one, because 768 is this app's mobile boundary everywhere and a rail at 700 would draw a rail and a phone dock together in the band between. Nothing was built out of §4.5, which is the finding |
| E4 | **§2.2 and §2.3's tautology sections remain unfixed**, which was half of the owner's sixth instruction. | rater | **FIXED** — all six of §2.2 and §2.3's hole. Each replacement is a render or a resolved value: `dom/key-legends.test.jsx`, `dom/hidden-section.test.jsx`, `dom/month-axis.test.jsx` (in Bengali, because in English a cut and the table both produce "Jan"), two cases in `dom/work-tile-marks.test.jsx`, an extended `scroll-containment.test.js`, and `test/css-cascade.js` — the resolver lifted out of `accent-texture.test.jsx` so the four byte-level matchers can ask for a value. Two live gaps fell out of it: the drawer's account row printed no key for `g p`, and E6 below |
| E5 | **The ✕ rule contradicts itself.** `PanelHost` reds it only when `dirty > 0`; `identityPicker.jsx` passes `closeDanger` unconditionally; CLAUDE.md states it flatly. One of the three has to give. | rater | **FIXED** — `PanelHost` gave. The `dirty` gate belongs to the TICK and only to it: the tick's arming answers "has something changed", the cross's colour answers "what does this press do", and asking both the same question signals one fact twice while leaving the second unanswered — and leaves a reader who has typed nothing unable to tell a form they may leave freely from a form at all. CLAUDE.md is amended to state the rule as it now holds; `cross-is-red.test.jsx` holds both surfaces side by side, and 3 of its 9 cases fail against the old gate |
| E7 | **Forty-three glyphs across seventeen files are typed characters, not the app's drawings.** `♥ ▲ ▼ ▾ ▸ → ↗ ✓ ✕ ⚠ ⌃ ⌄ ⇢ 🗑 ✎ ❞ ❑` — the sort arrows on three tables, the favourite mark on five screens, the arrow between an import's before and after, the tick in a menu row, the identity sheets' own row icons. CLAUDE.md's rule is flat and the repo had already fixed this exact class once, on the credit row, with nothing stopping the next one. | self | **FIXED** — all 43. Two new drawings were needed (`IconArrow`, `IconWarning`) and are documented in the glossary; `glyphs-are-drawn.test.js` is the ratchet, and three existing tests were keyed to the CHARACTER rather than to the feature and had to be retargeted — which is the same defect class in the tests |
| E6 | **The shared cascade resolver answered with an ANCESTOR's declaration.** `competes(sel, target)` reduced the CANDIDATE to its rightmost compound and then compared it against the whole TARGET, ancestors included — so `.drawer-item.active .review-dot`'s background resolved to `.drawer-item.active`'s fill. Every target it had been asked about was a single compound, where the two readings coincide. | self | **FIXED** — the target is reduced the same way as the candidate |

---

## I. The owner's reports of 5 September, second round

Four reports in one message, and three of them turned out to be one row in the owner's
library. The measurements below are from that library restored on a scratch server, never
from the seeded fixture — which has four cast rows, no tombstones and no unlinked
performers, so not one of these could have reproduced in it.

| # | Defect | Source | State |
|---|---|---|---|
| I1 | **"anand still doesn't open"**, a fourth report. The speaker chip on a film's own quote drew a name and opened nothing. | owner | **FIXED** in `400717d`. `work_cast` 188 on *Anand* — character "Anand", performer "Rajesh Khanna" — was `origin = 'removed'`. The reader had deleted the film's two provider rows and then typed both characters onto quotes of it; one came back and one did not, because adoption skips a key ANY row holds, tombstones included, and the reader's spelling of the other character folded to a different key. So the rule in force was not "a tombstone stays dead", it was **"a tombstone stays dead unless you spell the name differently"**, which nobody chose. A deleted row now comes back when this work's own quotes name it AND no live row already carries that name; the second half is what keeps a de-duplication deleted, and the owner's library has one of those (Geralt on *The Witcher 3*) which stays deleted. A refetch still cannot revive anything — that is enforced in `mergeProviderCast`, not here. `speaker_has_a_door_test.go` asserts the three facts the reader was looking at (the row on the list, the door on the chip, the film on the performer's page) and fails on the first of them without the fix |
| I2 | **"rajesh khanna still doesn't show any work"** and, in the second message, **"rajesh khanna is still appearing outside"**. His page said he was in no films while that film's quote credited him; and because the line had no cast row, the chip could not stack, so the performer printed on a PLAYED BY line of its own instead. | owner | **FIXED** in `400717d` — the same row. With 188 live, `PersonRoles` finds it (`actor_id = 62`) and the chip stacks: the pill reads **Anand / Rajesh Khanna** and the credit line is gone |
| I3 | **41 cast rows name a performer with no record behind them** — 38 TheTVDB, 3 Wikidata, across four titles, including both surviving performers on *Anand*. Their face opened nothing and their own page claimed no films. | self, from I1 | **FIXED** in `400717d`. Rows written by a fetch after `3.1.0-person-identity` had run and before the provider merge learned to link — a real window in 3.1.0's development, covered by neither. `onetime_3_1_0_cast_performer.go` gives each the record it names through the live `LinkCastRow`, skips tombstones (nothing can open a row nobody can see) and leaves a row the reader has re-pointed exactly where they put it. Two cases, both failing without the pass |
| I4 | **"the movie doesn't show director."** | owner | **FIXED** in `400717d` — and the first reading of this was wrong: the hero's DIR. chip draws, so I went looking for missing data and found every film in the library has one. The screen that does not show it is **Details**, where the credits had been folded into one row headed *People* printing the names and not which of them was which: "Hrishikesh Mukherjee — 4 characters", never saying he directed it. A name is not a credit until something says which credit it is. The pack's `filmRows` and `bookRows` give each its own labelled row and that is restored; `WorkPeople` stops drawing the credits it no longer owns, so there is one editor per fact |
| I5 | **"there is no cast carousel (with circular cast images as in the prototype)."** | owner | **FIXED** in `400717d` — `work-details-popup.dc.html:784-800`, billed at `:1092`, `:1116` and `:1160`: a round face per member under `Cast · N`, the character over the performer, between the fields and the ids. Two departures, both the standing rules' rather than this component's, and both argued at the component: the names **wrap** (the pack clamps them to two lines with `overflow: hidden`, and a name is never shortened), and the fade is **measured** by `Scroller` rather than switched on at five tiles. Reading the cast from the cast endpoint rather than off the record also closes the gap `onetime_3_1_0_quote_cast.go`'s own header names — that read adopts, so opening Details on a work whose People panel has never been visited now gives its quoted characters the rows their chips hang off |
| I6 | **"the spaced repitition dot is now on an orphan row. make it a four way wireframe icon (remembered, forgetting, probably forgotten, and not tested) and put in the bottom row (where the icons are) as the first icon."** | owner | **FIXED** in `400717d`. Both halves are the same complaint: a 7px disc has no shape, so its four states were told apart by hue alone — the one channel a reader may not have, and one the card already spends two controls away on its six colours — and a mark with no shape cannot join a row of glyphs, so it had been left on a line of its own. `IconRecall` draws one ring and how much of it is left, with a centre mark repeating the state at a size the ring cannot hold; it leads the action row, because it is the card's state rather than something you can do to the card. `recall-mark.test.jsx` strips every colour out of the four drawings and requires them still to differ |
| I7 | **"a little bit of 3d styling would do well on every screen. the details, cast screens are too flat now, compared to the app. make everything a little bit 3d, skeumorphic. use the elevation and also the wells."** | owner | **FIXED** in `400717d`, with the scope stated rather than implied. **Read against what the report compares:** the app's cards, buttons and bars have carried elevation and a lit edge since the material sets landed — that is the "compared to the app" half — and what had none were the surfaces the PANELS are made of. So three gained a body: a pressable row (`.cs-row`, drawn as nothing but a hover tint), a field on a panel sheet (`.inline-field-rows > .inline-field`, text between two hairlines), and a strip of tiles (`.cs-strip`, a flat run with no shelf under it). The elevation existed as `--lift`; the other two recipes were spelled inline in a dozen places at eight opacities in hardcoded rgba that ignored the palette's own `--sh`, so a new surface had nothing to reach for — `--bevel-hi`, `--bevel-mid` and `--well` name them once, in both modes. **The 34 hand-written lit edges already in the file are NOT swept, and that is a decision:** each is a value tuned to its own surface — .18 on a floating menu, .8 on a raised card, .06 on the same card in the dark — and flattening them onto one token would be a regression wearing consistency's clothes. `depth.test.js` reads the stylesheet for the three surfaces and both recipes, and ratchets the hand-written count so it may fall and never rise; 5 of its 6 cases fail against the file as it stood |
| I8 | **The screenshot harness left nine copies of the owner's library on disk.** `run-with-backup.sh` restores a real backup into a `mktemp` data dir and its whole privacy argument is that the dir does not outlive the run. | self | **FIXED** in `8419ac0`. The cleanup was `trap … EXIT`, which fires when the shell RETURNS — a run stopped with a TERM (a harness, a Ctrl-C, a supervisor) never gets there, so the scratch server kept running and its data dir stayed. Nine had accumulated across this session's interrupted runs, each a full restored copy of somebody's library; all nine were removed the moment they were found. The trap covers `INT`, `TERM` and `HUP` now, and because SIGKILL cannot be trapped by construction, the NEXT run sweeps any `mktemp` holding a `tippani.db` that nothing is serving — which is the only thing that can answer a kill. Found by looking after a run I had stopped myself, which is the only reason it was found at all: nothing failed, nothing was logged, and the harness reported success every time |
| I9 | **"Show me this" showed the reader something else, on every pass after the first.** A control that REPLACES the panel stack — a performer's page opened from a character's, the person picker, "Open the global record" — worked the first time in a tab and then dropped the reader two panels down. | probe | **FIXED** in `5fd0873`, corrected in `00f8945`. `usePanelStack().open` asked the browser to `go(-n)` and pushed the panel on the pop it got back, with a lander that refuses to push unless the stack has emptied — the discriminator that tells its own traversal from somebody's Back press. `go(-n)` lands wherever the entries actually ARE, and by the second pass the n entries below are the FIRST pass's panels, which carry a `tpPanelDepth` of their own: the pop arrived truncated to 2 rather than to 0, the lander correctly judged it not its own, and the open was abandoned. Measured on the owner's library at 1280, pressing the same performer's name three times: pass 1 opened `Max von Sydow · actor · 1 work`, passes 2 and 3 landed on `Details … POSTER … Fetch metadata`. **`make controls` found it and I did not believe it** — a first hand-check pressed the control once, saw it work, and called the probe wrong; it took replicating the probe's own sequence to reproduce. **The first fix was wrong and the second is the one that shipped.** Emptying the stack to `[panel]` is the faithful-looking reading of "replaces" and it breaks the way out: two panel entries remain in history under a stack of one, so `close()` walks back onto an entry recording a depth of 2, the truncation guard (`want < depth`) declines because 2 is not less than 1, and the reader presses ✕ and the panel stays — one broken control traded for another. What `open()` actually promises is that a control meaning "show me this" must not DEEPEN, so it replaces the TOP of the stack and touches history not at all: the depth is unchanged, every entry's recorded depth stays true, and Back from a person opened inside Details returns to Details, which is where the reader came from. The empty-stack case still goes through `push`, unchanged, because that case was never the defect. **jsdom is why this survived a 3,000-case suite**: `history.go()` there does not deliver a popstate the way a browser does, so every test of a replacing open passed on a mechanism that only ever worked once. `panel-open-replaces.test.jsx` asserts the PROPERTY — after `open`, the stack IS this panel — and 2 of its 3 cases fail against the mechanism |
| I10 | **The credit portrait never rendered, and three documents said it did.** It was passed as a SECOND `display` prop on the same `InlineField`, so JSX kept the later one and no `.cred-face` was ever drawn. | rater | **FIXED** in `4c39b1b`. Vitest printed `Duplicate "display" attribute` on every run of the suite and I did not read it — a warning nobody reads is a test nobody wrote. The commit that added the portrait claimed it, `codebase-audit.md` §4.6 recorded the row as a Match, and three `usePeople` fetches ran for nothing. One `display` now, and `work-details-order.test.jsx` asserts ONE `.cred-face` PER CREDIT NAME, which is the only shape that fails when the drawing is dropped rather than merely restyled; it fails against what shipped. §4.6 is corrected in place rather than quietly amended |
| I11 | **`panel-open-replaces.test.jsx` guarded nothing.** It asserted the number of `.tp-panel` elements, and `PanelHost` renders only the TOP of the stack — so the count is 1 whatever the stack holds. | rater | **FIXED** in `4c39b1b`. Mutating `open` to a plain `push` — the exact defect the file was written for — left three of its four cases green. It reads the stack DEPTH now, published from the harness on every render; the same mutation fails two of four. This is the owner's tenth instruction failing from the other side: a test that cannot fail is a test whose writer knew the fix and asserted its shape rather than the feature |
| I12 | **A game's studio was looked up in a film database.** `movies.director` holds a film's director, a show's creator and a game's studio (0040); the row's LABEL has changed with the medium for three releases and the FACE beside it was asked for under a constant `personKind: 'director'`. | rater | **FIXED** in `4c39b1b` — `personKindFor` reads `workKinds.js`, which already maps the column per medium because the work page's own credit chips read it. A second table of one fact is a second table to forget. The comment claiming the constant adapted is corrected rather than deleted, and `game-details.test.js` asks the three media |
| I13 | **Three smaller ones from the same rating.** The cast strip argued a departure against a strawman (the pack prints `not named`, not a blank); `.status-dot` was dead CSS whose help entry still called the control a Status dot; and the harness sweep looked only in `/tmp` while `mktemp -d` honours `$TMPDIR`, so on any machine that sets it the sweep found nothing and said nothing. | rater | **FIXED** in `4c39b1b`. The pack's line is printed on a medium that has performers and left off on a book, argued at the caller; the dead rule is deleted and the term renamed to Recall mark in both languages; the sweep looks where `mktemp` writes |

## J. The owner's reports of 6 September

| # | Defect | Source | State |
|---|---|---|---|
| J1 | **"expanded favourite card is still duplicating actor. the collapsed card is fine."** | owner | **FIXED** in `d48ed3f`. The chip names the character with the performer under it and the expanded tile drew a portrait credit for that same performer a few millimetres below — collapsed there is no credit row, which is the whole of why the collapsed card was fine. The rule this breaks was already written down and already enforced on the film frame, folded into a boolean of its own there; `creditsNotOnChips` is it written once for both, and the tile now credits only the names no chip carries. Name by name rather than all-or-nothing: a line can credit somebody the chips do not resolve, and they still need their portrait. Also on that tile: the expanded line printed `title · episode · character · timestamp` under a header naming the film and over a chip naming the character, while its own comment said the line was the locator — it is the locator now |
| J2 | **"as for cards in work pages, single character cards are fine. multi-character ones still has a separate actor line."** | owner | **FIXED** in `d48ed3f`. One character obeyed the rule and two did not, for one reason: the app held the character/performer pairing for the STORED SPEAKER alone, so every other name on a line came back with a character and no performer and the card had to fall back to a PLAYED BY line printing both performers again. `work_cast.actor` rides along with the picture now, through the same lookup and the same join, so every chip on every screen that draws them knows who plays the character it names — and gets their headshot as the same fallback the speaker's chip already had, which is why one chip of a pair wore Audrey Hepburn's face and the other a silhouette. `chip_names_its_performer_test.go` asks it of the READ a reader actually makes, which is where the fact was failing to arrive; `one-printing-per-person.test.jsx` asks both cards to print each performer once, and each mutation of the three pieces kills exactly one of its cases |
| J3 | **"the back breadcrumbs sometimes do this. ellipsis them"** — with a screenshot of a sub-sheet whose back crumb read `← V / William Ro`. | owner | **FIXED** in `d48ed3f`, **and this row's first version described the wrong failure**. It said the crumb printed over the title because it could neither shrink nor clip. Measured since: the word carried `overflow-x: auto` for a fade, and a flex item whose overflow is not visible has an automatic minimum size of ZERO — so it shrank, and it clipped, and it clipped **silently**, the name stopping mid-word with nothing to say it had been cut. That is exactly the failure the app's "never truncate a name" rule exists to prevent, happening under a rule written to prevent it, and the fade could never have prevented it: a 26px mask over a 13px word inside an 11ch button promises a drag a thumb in a button cannot make. The overlap was my reading of the screenshot rather than a measurement, and `run-panel-depth.sh` refutes it — forcing a name far longer than the key at 390, the old rules clipped with no mark on both heads that draw one and did not overlap. The key's `flex: none` is a real hazard (it cannot give up width and `.tp-panel-slot` clips nothing) and is fixed too, labelled as a hazard rather than as the report. **The ellipsis is an EXCEPTION to a standing rule and is argued rather than assumed:** a crumb is a signpost back to a screen whose own header has just printed that name in full, so it is the one text in the app where a name is not being read. `no-truncated-names.test.js` keeps the class in its list with the ruling beside it and requires a real clip; `crumb-stays-in-its-slot.test.js` asks the declarations that decide it; `run-panel-depth.sh` reads the rectangles in a browser and fails on both heads against the stylesheet as it shipped |
| J4 | **`metadata-sections.test.jsx` raced a fetch and lost once in a full run.** `mount()` waits for the section field, which draws before the counts behind it arrive; the coverage assertion was a synchronous `getByText` after it. | self | **FIXED** in `d48ed3f` — one `find` instead of one `get`. Recorded rather than waved off: it passed alone every time and failed once under load, which is exactly the shape `vitest.config.js`'s own header warns about, and calling that a flake is how a real hang gets ignored later |
| J5 | **`make controls` reported the app's one character surface as unreachable at 1280 and tested it fine at 390** — the two passes differ only in width, and the door does not, so the defect was in the measurement. | probe | **FIXED** in `dfd7a13`. `resolveSurfaces` chose the film to open by reading `GET /movies/{id}` and counting `d.cast`, which answers zero on a freshly seeded library: a work whose characters exist only as names on its quotes has no `work_cast` row until something ADOPTS them, and only `GET /movies/{id}/cast` does. So the probe fell through to the first film in the list — a game with no cast — and reported a door as broken that a reader opens every time. **The 390 pass passed for the worst possible reason:** the 1280 pass ran first and its own Details press adopted the rows, so by the phone pass there was a film to find. It also read only the first twelve of a newest-first list, and the fixture seeds films before shows and games, so those twelve were exactly the works with no cast. Both passes green now, with the Character panel opening at 1280 and its 22 controls tested |
| J6 | **A chip names a performer it cannot reach.** The chooser a character chip opens has offered "or the people" since the owner ruled on it, and gated that row's press on the performer's record id — which no payload has ever carried. It drew and declined on every real press. | rater | **FIXED** in `52585ef`. Survivable while the card printed a PLAYED BY line with a door on it; not once J2 took that line away wherever the chips already name them. `quoteSpeakerCast` and `store.LineFace` serve `actor_id` off the join both queries already made, and `chipRows` hands it to the door with the performer's name and face. **Five green cases said the door was built:** every case in `speaker-destinations.test.jsx` fed it an `actor_id` typed into a fixture by hand, which is the "a test that feeds in the answer" hole this repo has been caught by twice. The two new cases press a RENDERED chip and feed the door what the press produces |
| J7 | **The same duplication survived on a third surface.** A line opened from search is a header and then the whole card; the header printed the performers the card's chips name. | rater | **FIXED** in `52585ef` — the same helper, asked the same way, because a fourth reading of one rule is a fourth place it can stop being obeyed. That card was also never handed the reader's credit separators, so the two halves of one modal could disagree about how many performers a line credits — which is the split the rule is decided on |
| J8 | **`panel-depth.mjs` could not reach its subject and had been exiting 1 on every run**, while three places in the repo call it the guard for the go/push race. It pressed "a film page, a cast chip"; the cast moved into the Details panel when that screen was built to the pack, so the two chips left on a film page are the director's and open the older modal. | self | **FIXED** in `52585ef`. Found by extending it rather than by running it, which is the argument for running a guard on the schedule its subject changes on. It takes the reader's path now — Details, then a cast face — and measures what only a browser can see: that history counted both panels, and that the back crumb clips, is marked, and stays clear of the title, on both heads that draw a crumb, at 390, with a parent name forced far longer than the key |
| J9 | **J3's first telling described the wrong failure, and this row is the correction.** I wrote that the crumb printed over the title because it could neither shrink nor clip. | self | **NOT A DEFECT as described; the underlying report is FIXED** — see J3. The word could shrink: a flex item whose overflow is not visible has an automatic minimum size of ZERO. What it did was clip SILENTLY, which is the one failure "never truncate a name" exists to prevent, happening under a rule written to prevent it. The overlap was my reading of a screenshot rather than a measurement, and `panel-depth.mjs` refutes it at 390 on both heads. Corrected in the stylesheet, `ui.jsx`, `crumb-stays-in-its-slot.test.js`, `docs/PLAN.md`, J3 and the changelog. Kept as a row because the pattern matters more than the instance: a mechanism inferred from a picture and then written into six places is how a wrong story becomes the record |
| J10 | **Five prototype-fidelity changes shipped in `397c19c` with no test at all.** | rater | **FIXED** in `52585ef` — `Cast · none` rather than `Cast · 0`; the head still drawing after a FAILED cast read, which matters because the head is the only door to the cast editor; a book's tiles saying nobody plays them, in different words from a film's unnamed credit, because they are different facts; and a game's publisher drawing as a credit asked of the PUBLISHERS rather than the directors. Each mutation of the four kills its own case |
| J11 | **A leaked scratch server made the next run measure a dead run's library, and report a seeding refusal.** `make controls` exited 2 with "account already holds 22 book(s) — refusing to seed on top of it". Nothing was wrong with the app or the probe. | self | **FIXED** in `ec6e2e1`. A previous harness run had been stopped with a TERM; `trap … EXIT` fires when a shell RETURNS, so it never ran, the server kept 127.0.0.1:8128, and the new server could not bind — after which the harness healthchecked the PORT, got an answer, and seeded, logged into and measured the dead run's library. **This is I8 unswept across six siblings:** the trap was widened to INT/TERM/HUP and given a SIGKILL sweep in `run-with-backup.sh` alone, because that is the one that restores somebody's real library, and the reasoning applied word for word to the other six. They share `scratch-server.sh` now. **The cleanup was never the whole answer:** `scratch_require_free` refuses to start when something already answers on the port, because a leaked process does not announce itself as a leaked process and every guard downstream of the bind was happy. One stray server and one data dir were removed by hand when found; both held the seeded fixture, not the owner's library |
| J12 | **A citation of the design pack pointed at the wrong line, three times.** `credit: 'org'` was cited at `:1174`, which is `scopeTitle: 'The picker — a match is proposed, never applied'` in a different artboard; the real line is `:1067`. `twoCol = W >= 1180` was cited at `:1616` and is at `:1611`. A third was a paraphrase dressed as a quote. | rater | **FIXED** in `ec6e2e1`. A citation is how the owner's standing rule is served — nothing deviates from the prototype unless it is expounded — so a wrong one is worse than none: it reads as evidence and is checkable only by opening a six-thousand-line artboard, which nobody does. Found by a reviewer reading the pack, which is exactly the labour a test replaces. `pack-citations.test.js` reads every citation of the shape ``literal` (`:N`)``, resolves the artboard the way a reader does — the nearest file named before it, and only when that file IS an artboard — and requires the quoted words to appear within a few lines of the cited one. It found all three, and two false alarms of its own first cut, which are why it now tells a source citation from a pack one |
| J13 | **AI.md's counts went stale twice inside the session that recounted them**, once between the commit that fixed them and the commit two after it. | rater | **FIXED** in `ec6e2e1`, and converted from a habit into a guard. That file has said "recount rather than trust it" through six recounts and gone stale after five. `ai-counts.test.js` counts the Go test functions, the Go test files and the frontend test files the way the commands printed beside them do, and fails when the sentence disagrees. **The frontend TEST total is deliberately not checked:** `it.each` expands at run time, so a static count would be confidently wrong, which is worse than honestly stale — AI.md now says which of its four numbers to distrust |
| J14 | **The typescale ratchet cannot see the app's one deliberate clip**, and a floor of zero reads as "nothing in this app truncates". | rater | **NOT A DEFECT — recorded rather than repaired.** `typescale.mjs` visits ROUTES and measures each screen at rest; nothing behind a press is ever opened, so a panel's back crumb is outside its reach in both directions and its zero is not a claim about one. Extending it to walk into panels is a different probe from the one it is (`controls.mjs` is the one that presses things). What was wrong was the file reading as more than it measures, so `typescale-baseline.json` now says what it does not cover and names the three places the crumb IS checked |

## K. The owner's reports of 6 September, third round

| # | Defect | Source | State |
|---|---|---|---|
| K1 | **"the quotes and scene counts are a little misaligned."** | owner | **FIXED** in `79f033f`, **and the CSS was only half of it — see K7**, which is the half that was actually moving things. Two equal boxes, each centring its own [glyph][number][caption] group — so a wider group and a narrower one put six things at six offsets. Anchored to one edge, with the figure given a two-character column so a `1` and an `11` leave their captions in the same place, in `ch` because the figure is display type. `counts-align.test.js` reads the pair of declarations that decides it; jsdom lays nothing out, so a rendered sheet reports both boxes at zero and would "align" however they are declared |
| K2 | **"the 'in this work' section has duplicate and unnecessary prose."** | owner | **FIXED** in `79f033f`. The row said its scope three times — "In this work" as the label, "This work only" under it, "nothing written for this work" instead of its value — and was long enough that the label itself wrapped and clipped to "In this wor". The label already says the scope; the value says whether anything is there, in the word the Note row beside it uses |
| K3 | **"we don't need to write overly long proses like 'the only name this work uses'. it is obvious from there not being any alias."** | owner | **FIXED** in `79f033f` — the sub-line is the aliases or nothing. A row whose second line restates the absence of its own second line is a row that has learned to talk. The key went with it: an unrendered key counts against every language's coverage, and `locale-complete` is what said so |
| K4 | **"the home favourite chips directly opens the character. the work page chips gives the option. both should behave similarly. in fact this should be a repo directive."** | owner | **FIXED** in `79f033f`, and **made a standing rule** in CLAUDE.md: two things that look the same behave the same, and the behaviour lives in one function both screens call. Home's line was written before the chooser existed and never swept, so one pill did two things depending on the board — and neither screen was wrong on its own terms, which is why nothing caught it. `openCharacterDoor` is the worked example; the guard is that no second file may define it |
| K5 | **"the picker (when a chip is pressed) doesn't show any images."** | owner | **FIXED** in `79f033f`. `Face` resolves a stored path with `coverImgURL` and the caller resolved it first, so every row asked for `/api/covers//api/covers/…` and drew the browser's broken-image glyph. One resolver, at the component |
| K6 | **"the picker is full screen but then the menu that is opened is a popup. this is not escalation, but still feels weird."** | owner | **FIXED** in `79f033f`, and it is CLAUDE.md's second new rule: a question wears the same chrome as its answer. The chooser was a modal and every answer to it is a panel, so on a phone a full-screen sheet asked about surfaces that hug the bottom. It is a panel on the same stack now; the rows are one component, so the removal lists that ARE genuinely modal keep their modal. **The first test of this did not discriminate** — a modal rendered inside the panel still satisfies "the rows are in a panel" — so it counts the dialogs instead: one, not two |
| K7 | **Four glyphs ignored the size their callers passed**, across eight call sites. `IconBack`, `IconCheck`, `IconDelete` and `IconQuote` were declared `()` while callers wrote `size={15}` — React says nothing about an unknown prop on a component, so each drew at the 24px default. | self | **FIXED** in `d35f402`, and it is what K1 actually was: the two counts did not line up because `IconQuote` drew 9px wider than `IconDetails` beside it and pushed the number and the caption along. The CSS was a contributing cause and not the cause. `glyph-size.test.js` states the rule rather than the four: if ANY call site passes `size` to a glyph, that glyph's declaration takes it — the reverse is not required, because a glyph nobody sizes needs no parameter |
| K8 | **The chooser dismissed itself after an answer had already replaced it.** `choosePanel` passed `onDone={() => stack.back()}` — right for the modal it used to be, one entry too far for a panel: a row's own `open()` REPLACES the top, so `back()` popped the entry underneath. | rater | **FIXED** in `9db4d8d` — the call is gone, and the contract is stated at `choosePanel`: a row on that surface navigates, and a row that merely acts belongs in `ChoosePicker`. **Reported as closing everything; I could not reproduce that.** Measured on the owner's own library at 390, from Home and from a film page, with the `back()` line restored: the answer still landed and one panel stayed. So the removal is right on its own terms — dismissing a surface that has already been replaced is wrong however it happens to behave — and this row does not claim a reproduction it does not have. `speaker-destinations.test.jsx` now presses each ANSWER (four cases that never existed), and the one that discriminates asserts `history.back` is not called: jsdom delivers no popstate for a traversal, so every other case there passes against the old line. `run-panel-depth.sh` presses an answer in a browser on every run |
| K9 | **Nothing tested the outcome of the chooser.** Four new files asserted the question's chrome, its `src` strings and two source greps; none pressed a row. | rater | **FIXED** in `9db4d8d`. This is the third time this session a guard was written about the shape of a thing rather than what it does, and the second time jsdom's missing popstate is what let it pass — recorded together with K8 so the pattern is legible rather than the instance |
| K10 | **K2 shortened a string where the box was the defect.** `.cs-row-meta` was `flex: none` with no scroller, so a long value took its width out of the row and squeezed the label's scroller until "In this work" clipped. | rater | **FIXED** in `9db4d8d`, and CLAUDE.md already said which repair was wrong: "If text clips, grow the box." The value gives way now (`flex: 0 1 auto`, a ceiling as a share of the container) and scrolls under the fade rather than being cut — it is a NAME on some rows, so the standing rule binds it. Guarded with a real description in the fixture rather than the empty one |
| K11 | **Two guards written this round could not fail.** The empty-value case allowed anything under 40 characters and the string it was written against is 29; the glyph case read a SIGNATURE, so a glyph declared `({ size })` that never applied it passed. | rater | **FIXED** in `9db4d8d`. The first now requires the two empty values on one sheet to be the same word — which is what was actually being asked. The second has a sibling in jsdom that RENDERS every sized glyph and reads the width off the element: `glyph-drawn-size.test.jsx`, which fails on exactly the mutation the pure one cannot see |
| K12 | **K4's guard was a 700-character window over two named files** — a directive written repo-wide, enforced on the two screens that already obeyed it. | rater | **FIXED** in `9db4d8d`, in two passes and the first was wrong: widening it to every `onOpenCharacter` flagged the cast EDITOR's row and the cast strip's tile, which are not the control the owner meant — a row in a work's own cast list has one destination by construction and pushes on purpose. It is scoped to the PILL now, and which components draw one is DERIVED (they are the ones rendering `SpeakerChips`), so a fourth card joins the rule by drawing pills rather than by being listed. The derivation was itself wrong once — a regex for a function body stopped at the first `}` in column 0, which left `FavouriteTile` out of its own guard and made two cases pass on an empty set |
| K13 | **`panel-depth.mjs` reported nothing as ok, twice over.** Its header said it checks two things when it checks three, and the third case had two branches that logged a `note` and left the failure count alone. One of them fires by DESIGN: a character with a single live candidate opens that record directly, so pressing whichever chip came first and logging "nothing was pressed" measured the ORDER of the fixture and called it a run. | rater | **FIXED** in `c3b8045`. The probe walks the live chips until one ASKS and fails when none does — a fixture that has drifted out from under a probe leaves the probe knowing less than before it ran, and a note in the log is how that reads as a pass to whoever is looking at the exit code. The ok line now names the chip and how many answers it found, so a run says what it saw rather than that it finished. **Proven by mutation:** asked for 99 answers instead of 2 it prints the FAIL and exits 1; as shipped it exits 0. This is the file's own rule, which it had been breaking since it was written — the same rule that put the FAIL in its cast-face branch |
| K14 | **`.cs-row-meta`'s repair had no guard that could see it.** Reverting `flex: 0 1 auto` to `flex: none` — the K10 defect, exactly — left all 3,147 tests green. | rater | **FIXED** in `c3b8045`. The nearest case asserted that the value renders inside a `name-scroll`, which is a CLASS: a shape, and shapes survive the mutation that matters. `counts-align.test.js` asks the declaration directly — a `flex` that can shrink, over a `min-width` of zero. **The fourth time this session a guard described a shape rather than a behaviour**, and it is recorded again because the instances keep differing while the mistake does not |
| K15 | **The figure's column was two characters wide**, which aligns a 1 against an 11 and then lets a 100 push the caption along again — so K1 was fixed up to 99 quotes and not past it. | rater | **FIXED** in `c3b8045` — three characters, which is where a character's quote count actually stops on a real library. The **rejected alternative** is a column wide enough for a number nobody has, which buys permanence with row width the names need. The guard requires at least three and requires it in `ch` or `em`, so it states the rule and not the value: a later hand may widen the column, and may not shrink it back under the case the owner reported. The changelog's claim was corrected with it — "line up with each other" was true to 99 |
| K16 | **Three departures from the artboard were argued only in a CSS comment.** The counts row stops centring (`character-popup.dc.html:688`), the figure takes a column (`:692-693`) and a row's value can shrink (`:496`). | rater | **FIXED** in `c3b8045`. The owner's standing rule is that nothing deviates from the prototype unless it is expounded upon in detail, and CLAUDE.md names `docs/PLAN.md` as where a design departure goes — a comment beside the line is right for whoever is reading that rule and wrong for anyone asking what the app owes the pack. Each of the three now carries its measurement, the symptom it was reported as, and the alternative rejected. **And this row over-claimed its own guard when it was written:** it said `pack-citations.test.js` already held the three, and at that commit the guard did not read `docs/PLAN.md` at all and the three were written in a shape it cannot pair. Both were closed in `0a046f2` — see K19 |
| K17 | **`docs/ui-glossary.html` shipped stale in `9db4d8d`**, embedding `.cs-row-meta{…flex:none}` — the exact declaration that commit replaced. | rater | **FIXED** in `c3b8045`. The generated document is built from the SHIPPED stylesheet, so any CSS change without `make glossary` leaves it describing the previous build, and this one described the defect as current. **The control exists and is in the right place** — CI runs `npm run glossary:check` — so this is a row about a habit and not about the repo: it is the third generated artefact this session to go stale after a change, after AI.md twice, and the pattern is what the row is for |
| K18 | **J11's fix left the case it was written for uncovered.** A scratch server that outlives its shell goes on holding `tippani.db` open, so `fuser` answers yes, so `scratch_sweep` steps over the very directory it exists to remove — and the data dir a `run-with-backup.sh` run restored somebody's real library into stays decrypted on disk with nothing that will ever clean it up. `scratch_require_free` surfaces it only when the NEXT run wants the SAME port. | self | **FIXED** in `7995eb0`. The sweep could not tell an orphan from a concurrent run because it was asking the directory, and the two look identical from there; they differ from above — a concurrent run's server has its `run-*.sh` as its parent, an orphan's shell is gone so it has been reparented to init. A dir whose only holders are `<mktemp>/tippani` processes with PPID 1 is now stopped and removed, and a dir anything else is in is still stepped over, which is what keeps a concurrent run safe. `scratch-sweep.test.js` runs the real shell function against a `TMPDIR` of its own with three fixtures, and each of the three mutations — the old skip-if-in-use, dropping the binary check, dropping the parent check — kills exactly one case. **Found by the guard rather than by a leak:** a second `make controls` refused to start because the first was still running, which is `scratch_require_free` behaving correctly, and reading why is what showed the sweep's blind spot. Its fixture's first cut was itself wrong — a `while :; do sleep 1; done` holder spawns a `sleep` that inherits the descriptor, so `fuser` named a foreign holder and the sweep rightly declined |
| K19 | **The citation guard skipped the one document citations exist for.** `pack-citations.test.js` reads `web/frontend/src/` and `docs/plans/*.md`; CLAUDE.md names `docs/PLAN.md` as where a design departure goes, and that file was not in the list. K16's three new citations were also written in a shape the resolver cannot see — ``literal` at `file:N`` rather than the repo's ``literal` (`file:N`)`` — so the file scanned to zero citations and would have reported nothing either way. | self | **FIXED** in `0a046f2`. Both halves, because either alone leaves the same hole: `docs/PLAN.md` is in SOURCES and the three citations are in the convention. A guard that skips the file it exists for is the wrong half of the tree, and "no citations found here" is indistinguishable from "all of them check out" unless something says which. Proven by mutation: moving one citation to a line the artboard does not have fails two cases naming the file, the literal and the line it actually reads |

| K20 | **"the picker (when a chip is pressed) doesn't show any images" was half fixed.** The first half was a resolver applied twice (K5). The second half is a field that never left the card: a chip's own face climbs still-then-headshot, and the door was handed the headshot alone — so a character photographed IN THE ROLE with no separate portrait of their performer had a face on the card and a silhouette on the sheet the card opened. | rater | **FIXED** in `7c18acd`. Invisible on this library because half of it is silhouettes by design, so the sheet looked exactly like a sheet that is working. **Five green cases did not see it** for the reason this suite's own header names: every one of them fed the door an object typed into the test file, so the field being absent from the chip's PAYLOAD could not show. The new case renders a chip, clicks it, and takes what the component produced — and it asserts the resolver ran exactly once, which is K5's half of the same report |
| K21 | **The citation guard read about a tenth of the pack references in the tree.** Its pattern requires the quoted words to sit immediately before the line, and most references separate the two with a clause of prose. | rater | **FIXED** in `7c18acd`, with a second tier rather than a looser pattern: widening the pairing regex is what would let a citation pair with an unrelated literal fifteen lines up. EVERY backticked reference must now point at a line the artboard has; only the tight ones are also asked whether the words are there. 10 references checked became 81, and a floor requires the wide tier to stay the larger of the two. Reading a tenth of them and calling the convention guarded is how J12's wrong-line citation got in |
| K22 | **The register's own header kept a second copy of the commit list, and it went stale inside the commit that filled in nine commit names.** | rater | **FIXED** in `7c18acd` by deleting the list rather than extending it. Every row names the commit that closed it; a second copy at the top is one fact in two places, which is what this repo's seven-document rule exists to prevent and the same shape as AI.md's counts going stale six times |
| K23 | **A character's name sat in a box that could not shrink, on the sheet a chip opens.** `.cs-choose-meta` was `flex: none` and carries `a.character`, so a long name took its width out of the row before the label got any — which is K10 one surface over. | self | **FIXED** in `7c18acd`. Found by grepping the sheet's other `flex: none` slots after K10 rather than by a report, which is the point: the fix had been landed on one class while the same shape stood beside it, and that is how a fix becomes a special case. The guard states the rule over a LIST of classes and a third joins by being listed; `panel-depth.mjs` writes a name far longer than the row into every value slot and measures what the stylesheet does with it, because a declared ceiling and a cascade that overrides it are the same document to jsdom |
| K24 | **A script truncated this register to zero bytes.** `io.open(p, 'w')` is evaluated before the argument written into it, so a formatting error in the argument left the file open, emptied and unwritten. | self | **NOT A DEFECT IN THE APP — recorded because the register is the session's memory.** Restored from `761ad27` and the three uncommitted edits re-applied from the transcript; nothing was lost. Kept as a row because the failure mode is general and silent: every edit in this session that rewrites a document whole has the same shape, and the only reason this one was caught is that the next grep printed nothing |
| K25 | **A property the stylesheet declares never reached the stylesheet that ships.** `.tp-scrim` wrote `backdrop-filter` and then its `-webkit-` twin; the build kept only the twin. Every CSS guard in this repo reads `src/index.css` — the author's intent — and the browser reads `web/dist/assets/*.css`, so nothing could see it. | self | **FIXED** in `35f4849`. Found by measuring rather than by reading: `getComputedStyle(scrim).backdropFilter` came back `none` in a real browser on the owner's library, and the screenshot showed the page above an open sheet sharp. Ordering the `-webkit-` twin FIRST keeps both. **This is what made L3's row wrong twice** — the blur was in the source the whole time and had never once reached a screen. `prefixed-pairs-survive.test.js` asks the BUILT stylesheet whether every twin's standard property survived; it fails on exactly the reversed order and nothing else. The class matters more than the instance: nothing errors, nothing warns, and the rule is simply absent |


## L. The owner's reports of 6 September, fourth round

*Two phone screenshots: a character panel over a film's quote list, and a film's Details
panel. The panel work is one change with four parts — a surface that takes focus should
take it fully — and the details work is two.*

| # | Defect | Source | State |
|---|---|---|---|
| L1 | **"stop scrolling screens that are not in focus."** The app behind an open panel still scrolls: the quote list visible behind the Delia Surridge sheet moves under a drag meant for the sheet. | owner | **FIXED** in `7ac3e51`, **and the cause was not the one I first wrote down.** I recorded this as the panel stack never having joined a rule six other overlays follow, and then found `PanelHost` had been calling `useBodyScrollLock` since it was written. The hook hid the overflow of `<body>`; the element that scrolls a standards-mode document is `<html>`, so the lock was freezing a box that is not the scroller and the page went on moving. Nothing in the source looked missing, which is why the guard asks the DOCUMENT what it can still do rather than asking the component what it calls. The prototype locks both (`book-detail.dc.html:15`) |
| L2 | **"the chevron from bottom locations is visible above the popup as well."** The to-top control draws over an open panel, and answers to a surface that is not in focus. It should appear only when a long scrolling surface **that has focus** has been scrolled significantly down. | owner | **FIXED** in `7ac3e51`. The key is `position: fixed` at a higher layer than the scrim, and its gate reads `window.scrollY` — so it both drew over the sheet and offered to scroll a surface nobody was looking at. **Derived from the lock rather than listed beside it:** the scroll lock now publishes whether anything is covering the page, and `useBackToTop` consults it, so an overlay joins both rules by locking and cannot join one and forget the other. The gate on how far is unchanged — it was already the prototype's (`book-detail.dc.html:1631`) |
| L3 | **"also introduce focus blur, on desktop and mobile both."** Nothing behind an open panel is blurred, so the sheet and the screen under it read as one plane. | owner | **FIXED** in `35f4849`, **and this row has now been wrong twice, which is why the mechanism is written out rather than summarised.** (1) I first recorded it as "nothing is blurred". (2) I then *corrected* that to "a panel WAS blurred and was the only thing that was", because `.tp-panel-scrim` declared `backdrop-filter: blur(10px) saturate(.78)` in the source. (3) Measured in a real browser on the owner's own library at 390, `getComputedStyle(scrim).backdropFilter` came back **`none`** and the strip of page above an open sheet was sharp in the screenshot. **The first telling was right and my correction was the error.** The build was shipping ONLY the `-webkit-` twin: written standard-first, the minifier's collapse kept the last of the pair and dropped the standard property, so no guard in this repo could see it — every CSS guard here reads `src/index.css`, which is the author's intent, and the browser gets `web/dist/assets/*.css`. Ordering the twin first keeps both. **The blur is now on `.tp-scrim`, so every overlay has it** — the panel, every modal, every picker, the add sheet, the search overlay — with the lower alpha that pays for it, which was the substance of the change either way. `prefixed-pairs-survive.test.js` asks the BUILT stylesheet whether every `-webkit-` twin's standard property survived, because the class of defect is silent: nothing errors, nothing warns, the rule simply is not there |
| L4 | **"in mobile, make the popups fill the entire width and start from the bottom with edgemask if needed to be scrolled… scrolling down will close the popup now."** A panel on a phone is an inset card, not a bottom sheet. The behaviour to follow — behaviour only, not the screens — is `book-detail.dc.html`'s mobile work-details popups. Pending edits keep the warning they already trigger. | owner | **FIXED** in `b964745`. Below the desk breakpoint the panel already hugged the bottom and still kept 12px of scrim down each side and 30 beneath, so on a 390px screen it was an inset card with a hairline of page round three edges — a dialog that did not quite land. A phone breakpoint takes the full width and squares the bottom corners, after the pack's own (`position:absolute;left:0;right:0;bottom:${this.state.kb ? KB_H : 0}px;` — `book-detail.dc.html:4207-4211`). **The edge mask was already there:** the body is a `Scroller axis="v"`, which measures and fades. **The strip of page above the sheet is kept deliberately** — it is what says the reader is on top of something rather than on a new screen, and it is what the scrim's blur is drawn on. The drag takes the SAME guarded exit as the ✕ and Escape, so the unsaved question is asked; that case lives with the other three ways out rather than beside the gesture, and mutating `guard(back)` to `back` kills it. From the TOP of the body only — a downward drag anywhere else is the reader reading — and touch only, because a wheel emits the same delta while reading. `swipe-down` is promoted in the gesture library, which is what caught it: a guard reading `onTouchStart|touchmove` for a claim about PINCHING broke in the right direction and said the wrong thing, and now asks about pinch alone |
| L5 | **"the cast (and also the works page in actor/character pages) images hug the top of the well, needs slightly more breathing room."** | owner | **FIXED** in `7ac3e51`. Both rows showed 4px of shelf above the artwork and 10px below, because the well pads itself and the row pads itself and neither was written knowing the other — so the covers read as pushed against a lid. 10 and 10 now. The guard states the rule (as much shelf above as below, and enough of it to see) and SUMS the two rules, which is why hugging was invisible in a diff that only ever touched one of them |
| L6 | **"IDs can merge with links with option for a custom link."** A work's Details draws `LINKS` and `IDS` as two sections; a provider id IS a link to that provider, so they are one list with two ways of being written down, plus a way to add a link the app has no provider for. **The owner's ruling on the shape:** ids stay ids — the pill opens the provider, the pencil still edits the id — and *"for the user, this will be equivalent to the people screen links"*, so the merged section is the one a person's page already draws, not a second thing that resembles it. | owner | **FIXED** in `95cb124`. One section headed Links, holding a pill for every id the record holds and every address the reader has added, each opening the page it names. **An id stays an id** — mono voice, the provider's mark, the provider's page, and the head's pencil edits it as an id, because the columns are what re-verify and the metadata fetch read. Only the reading joins. **Two doors, both already precedented on this screen:** the ＋ at the end of the row opens the links panel (the paste box, plus the pages the record can already address from its own ids), and the head carries the verb that edits the ids, exactly as `Cast · N` does eight rows up. **Nothing new was built for it** — `PillRow` and `SectionHead` are the components a person's page draws its own links section with. **The claim was wider than that when written and is corrected here:** `linkRows` is `workLinks.jsx`'s and no people screen calls it, and that screen's door is `ProviderLinkDialog`, which a work's ＋ does not open — it opens the links panel, whose paste box and derived-provider list do the same job for a work and are not the same component. The `Links` form row and the `Ids` heading are gone, and with them two locale keys nothing renders any more. Five cases describe the merge and pressing one of them, and the four suites that reached the old doors were re-pointed rather than deleted — one of them was asserting that the Cast head is the ONLY head with a verb, which is no longer true and now asks for the Cast one by name |

| L7 | **The chevron was silenced everywhere instead of following the surface in focus.** L2's fix hides the page's key while any overlay is up, and stops there — so a panel body scrolled a long way down offered no way back to its own top, which is the case the owner's own sentence describes: "the chevron will only appear when a long scroll surface **is in focus** and has been scrolled significantly down". | rater | **FIXED** in `677e3ea`. **The pack settles it and says why in as many words:** its chevron watches `e.currentTarget` — "THE SCROLLER IS THE ELEMENT THAT SCROLLED, not a ref that happens to exist… the key fired scrollTo on a panel that was closed and the page never moved" (`book-detail.dc.html:1620-1622`). `useBackToTop` takes an optional target now and answers to that box; the overlay gate stays the DOCUMENT key's alone, because a key given a target IS the surface in focus and the gate would silence exactly the case asked for. `BackToTop` moved from `App.jsx` to `ui.jsx` so a panel can draw one, and draws it INSIDE the panel — the page's is fixed to the viewport corner, and on a desk the panel is a card in the middle of the screen with nothing of itself in that corner |
| L8 | **`make frame-scroll` has been failing two of its four widths since 2 September, and nobody ran it.** It asserts the metadata rail sits BESIDE its body at 1280 and 980; `a67f65d` — "the page fits a phone" — deliberately removed `.meta-frame`'s two-column track, on the argument written at `.meta-rail`: one shape for the sections at every width, because a screen with two ways to draw "these are the sections" has two focus rings and two active states. | self | **FIXED** in `677e3ea` — the probe expects what the app draws. **This is J8 again on a different probe** (`panel-depth.mjs` pressed for a cast that had moved into Details), and the lesson is the same one: a guard is only as current as the last time somebody ran it, and a probe outside `go test`/vitest is run by a person deciding to. Found by running all five browser probes at the end of a round rather than the one the change touched. **AND THIS ROW WAS WRITTEN BEFORE A GREEN RUN SAID SO,** which is L8's own lesson turned on itself: the commit it names corrected ONE stale expectation and the probe went on failing — first on the phone's metadata control (`53316e7`), then on two hero rows asserting a rule no artboard states, and it is red still on M2. A row that reads FIXED off a reasoned change rather than off a completed run is the register lying quietly |
| L9 | **The approved shape for a custom link included an optional label, and the link takes a URL alone.** The owner chose the option worded "\"Add a link\" takes a URL with an optional label"; `PasteLink` takes a URL, and `linkRows` names an unrecognised link by its host. | rater | **FIXED.** The cost below is what the row was holding it for, and the design is what pays it: **there is no second thing stored and no migration.** A work's links are ONE free-text field of newline-separated URLs (`links`), which is also how a person's and a character's are stored, and how `mergeLinks` folds a fetch into what the reader typed. A label per link needs a second thing stored beside each URL — a new column with a parallel list, or a syntax inside the field — and either changes what every reader and writer of `links` means, on three record kinds, with a migration for what is already there. **What the reader loses without it:** an unrecognised address reads as its host (`example.org`) rather than as what they would have called it; a recognised one already reads as its provider. **The over-claim goes with this row:** `95cb124`'s message says the people screen's components were reused, and the one it did NOT reuse is that screen's own door, `ProviderLinkDialog` — the work's ＋ opens the links panel, whose paste box and derived-provider list do the same job for a work, but they are not the same component. **What landed:** a line may carry `<url> | <name>`, and a pipe has never been legal in an address nor written into this field, so every line already stored reads exactly as it always did. **A space could not be the separator** — this field has always whitespace-split, so `a.com b.com` on one line is TWO links and always was, and reading the tail as a name would quietly rename somebody's second link; the case that says so is in `link-names.test.js`. The reader's name outranks the provider's wherever a link is drawn. **The two functions that REWRITE the whole field are where a name goes missing** — `mergeLinks` on a metadata fetch, and the panel's own append — and both are guarded; the append case was written only after a mutation proved the first draft did not catch it, because nothing in the fixture had a name to lose |

## M. What running every probe turned up

*L8 corrected one stale expectation in `make frame-scroll` and the run then got far enough
to report six more. The probe has never passed: its expectations were written in
`aaebda1` (1 September), the same commit that unified the work header, and nothing has run
it since.*

| # | Defect | Source | State |
|---|---|---|---|
| M1 | **`make frame-scroll` reports six failures on the work-detail hero, and one root cause under five of them.** Measured: `books 1440 cover 150px stacked`, `books 900 132px beside`, `books 390 96px beside`, `movies 1440 150px stacked`, `movies 900 132px beside`, `movies 390 96px beside`. The probe wants a film BESIDE at 1440 (it is stacked), and both kinds STACKED at 390 with a cover of at least 120px (they are beside, at 96px). It also reports "the compact bar never appeared at any height". | probe | **FIXED** in `53316e7` and `7ab913c`. **The 390 case is the probe's error, not the app's:** `index.css:5982-5992` restates the phone header's three numbers and cites them as the pack's — "A 96px cover rather than 132 (the board is an object beside the facts here, not the header itself)". So beside-at-96 is what the pack draws on a phone and the probe is asserting a stacked hero the pack never asked for. **The 1440 case and the compact bar are one question and it is still open:** the bar is drawn only when the hero column has SCROLLED (`works.jsx:1761`, gated `scrolled && !mobile`), the probe scrolls `.tp-detail-hero`, and if the app no longer puts a film's hero in that two-column column at 1440 then the column does not scroll, the bar never appears, and both failures follow from one change. Whether the app is right to stack at 1440 is a question for the artboards, and was read there rather than guessed. **CLOSED, five of six:** the artboard branches on the WIDTH and on nothing else — `heroSplit: twoCol || phone` a column, otherwise a row (`book-detail-wide.dc.html:4644-4646`), with `const phone = W < 700` (`:1596`) and `twoCol = W >= 1180`. So stacked at 1440, beside at 900, and the KIND has nothing to do with it: "a film is beside at 1440 where a book is stacked" was the probe's own sentence and no artboard says it. Below 700 the two artboards genuinely differ and the one drawn for a phone wins — a 96px board BESIDE the facts (`book-detail.dc.html:97`, `:3268`), which is what the app draws and what `index.css` already cites. The cover band is two numbers now because the pack draws two. **CLOSED ENTIRELY:** the sixth was M2, which this row called the app's and which measurement showed to be the probe's as well — four rows all at 1440, the one width where the compact bar cannot happen. `make frame-scroll` exits 0 |
| M2 | **The compact bar was reported as unable to appear at any real window height.** "when the hero section is scrolled down, the poster, title and author needs to morph into a small top bar in that section." | probe | **NOT A DEFECT, and this row's first telling was wrong twice over.** I measured at 1440 alone, found the marker six pixels short of its threshold, and generalised that to every width — then left the row open on the strength of it, which is an overstatement doing the work of a reason. Measured across widths on the seeded library: `1000x700 room 444px marker 215px down scrolled bar=true`, `900x600 room 759px marker 215px scrolled bar=true`, `1440x900 room 0px marker 354px bar=false`, `1440x520 room 168px marker 354px bar=false`. **Below 1180 the hero shares the WINDOW's scroll, the title leaves the top, and the bar arrives** — costing no layout (`moved 0px`) and keeping exactly one copy of the title at rest. **At 1180 and up the pack's two-column frame gives the hero its own column** (`heroSplit: twoCol || phone`, `book-detail-wide.dc.html:4644-4646`) and that column does not overflow at a real window height, so the hero never goes away, there is nothing to repeat, and the bar's absence is correct. The PROBE was the thing at fault: four rows all at 1440, the one width where it cannot happen. It visits the widths where it can now, so a run can report having SEEN the bar arrive rather than only having failed to see it — and `make frame-scroll` exits 0 for the first time since `aaebda1` on 1 September |
| M3 | **A control was described everywhere and rendered nowhere, and this row first blamed the wrong commit.** `677e3ea` DID add `<BackToTop …/>` along with the hook, the placement and the CSS; `53316e7` — whose message is entirely about `frame-scroll.mjs` — deleted it, because a rating pass was mutating the live working tree to prove a guard while the session was committing from it. Every one of the six hook cases stayed green. | self | **FIXED** in `5dc972b`. The cases were written where the rule is DECIDED (`useBackToTop`) and nothing asked whether `PanelHost` was wired to it, which is the shape-versus-behaviour hole this session has now been caught by five times, in a new place each time. Two cases in `panel-dirty-guard.test.jsx` render the panel and look for the key inside it, and removing the element kills exactly those two |
| M4 | **The merged row could draw one destination twice.** An id's pill opens the address the app BUILDS from it — `imdb_id`'s href is `providerURL('imdb', item)` — and the reader can paste that same address into the links panel, where it is stored as a link. Concatenated, the row then names one page twice, which is the standing rule the merge was made to serve. | rater | **FIXED** in `5dc972b` — the pills are unique by URL and the ID wins, because its pill carries the record's own number under the provider's mark where the link's would carry a host. **The offers list was left alone on purpose:** filtering `derivedLinks` by what the row shows as well would empty the "pages this record has" list that handoff §1.3 asked for, and it is not needed — a page added from there now folds into its id's pill rather than sitting beside it. That half was written, measured against five existing cases it broke, and reverted |
| M5 | **A control was argued at a width where it cannot draw.** The panel's key was rendered at every width with a paragraph explaining where it sits on a desk; `.to-top` is `display: none` until the phone query, which is the page key's own scoping ("a desk has a scrollbar to drag, a Home key"). | rater | **FIXED** in `5dc972b`. The key's STATE is gated on the phone — `useBackToTop` is handed `enabled: !!panel && phone`, so it never arms elsewhere — and `.to-top`'s own `display: none` keeps it off a desk. The element itself still renders at every width, which is why a dom case can find it under jsdom's width-less matchMedia; saying "the render is gated" was loose. The placement rule is gone with the argument for it — on a phone the panel IS the bottom of the screen, so the viewport corner is the panel's corner and the page key's `fixed` was already right. A control argued into existence at a width it cannot appear at is a comment, not a control |
## O. The owner's two minor defects, 6 September

| # | Defect | Source | State |
|---|---|---|---|
| O1 | **"a character does inherit the image of the actor, but that is valid only for the chips on display. nowhere else. here bhaskar bannerjee does not have his image, Amitabh bachchan has. so the picker should respect that."** | owner | **FIXED** in `7f726dc`. `openCharacterDoor`'s local row read `sp.image || sp.actor_image || ''`, so a character with no picture of their own wore the performer's — and the picker is the ONE place the character and the performer sit side by side, which made "this character" and "the performer's own record" two rows showing one man: the question drawing its own answer. The row shows the character's own picture or a silhouette now. **The chip keeps the loan and the ruling says so:** on a card there is nothing beside it to confuse it with, and a face beats no face. The case presses a rendered chip whose fixture has an `actor_image` and no `path`, and asserts the character's row draws NO image while the performer's draws theirs |
| O2 | **"the quote and scene are not taking the same width as other fields. and also the gap between fields are not equal (should be equal)."** | owner | **FIXED** in `7f726dc`. Measured in a browser on the owner's library at 390 before touching anything: the three wrappers agreed at 364px and their insets did not — rows 4px a side, facts 6px, counts 10px — so the rows drew 356px wide, the facts 352 and the counts 344. The counts also padded 2px above and below where the rows pad none, which is the whole of the uneven gap: two rows meet at a hairline and a row meets the counts across 2px of nothing. **One token, `--cs-inset`, read by all three**, because CLAUDE.md's rule is that spacing is a constant and "a step typed into a row is a bug" — three steps in three siblings is that bug three times. The guard asks that they read the TOKEN rather than that they equal 4px, so a later hand may move it and may not go back to typing a number per block |



## N. A phone sheet you can drag

*The owner, over a screenshot of a bottom sheet with a grab handle: "see your own design
for the popup. this is ideal. the small bar on top ensures that this is intuitively
draggable. the whole thing is responsive to drag, and has predefined anchors. can you do
this for the popups in the app?"*

| # | Defect | Source | State |
|---|---|---|---|
| N1 | **A phone sheet has one height and a threshold, where it should have a handle, live drag and anchors.** What is there: full width from the bottom, rounded top corners, an edge mask on the body, and a drag down from the top of the body that dismisses once it passes 90px. What is missing: the handle that says it is draggable, the sheet following the finger rather than jumping at a threshold, and more than one height to settle at. | owner | **FIXED.** The rulings are kept below because they are the specification.  **Anchors:** natural height → 76% → 94%, and pulling down from the smallest dismisses. 76 and 94 are the PACK's own two numbers (`max-height:calc(${sheet && sheet.form ? '94%' : '76%'}` — `book-detail.dc.html:4207-4208`), which it picks by KIND; this app has one panel that is a list and a form at once, so it could not pick, and anchors mean it does not have to — **which retires the departure `docs/PLAN.md` argues for the fixed strip.** **Grab area:** the handle and the header always; the body only when it is scrolled to its own top, because anywhere else a downward drag is the reader reading. **And tapping outside dismisses** — already true (`backdropClose` through the guarded exit), and on a phone "outside" is the strip of page the sheet leaves above itself, which is what that strip is for. **A departure either way:** the pack draws no handle and no drag at all, so the affordance and the gesture are argued in `docs/PLAN.md` rather than assumed | **What landed:** `useSheetDrag` replaces `useSwipeDown` — the sheet follows the finger, `sheetAnchors.js` decides what a release means with no React in it, and `.tp-sheet-grip` is a 44px strip around a 36×4 bar. A release is judged by PROJECTION rather than a threshold, which is the difference between a sheet that feels thrown and one that feels dragged; a velocity no thumb can reach is clamped, because `dy/dt` between two samples that arrive in the same tick is a division rather than a flick and would throw away a sheet the reader was nudging. **Three layers, because a drag has three separable halves:** the arithmetic in `sheet-anchors.test.js`, the wiring in `sheet-from-the-bottom.test.jsx` (both of which must FAKE a height — jsdom lays nothing out), and `make sheet-drag`, which runs the gesture in a real browser. With the drag's own `pointerdown` listener removed the probe prints two FAILs and exits 1. The departure is argued in `docs/PLAN.md` and supersedes the fixed-strip entry rather than deleting it |

---

## P. The owner's reports of 6 September, fifth round

| # | Defect | Source | Status |
|---|---|---|---|
| P1 | **"in metadata selections: replace the single tick at the top with double tick. the single tick feels like 'ok' and not 'multi-select'. this is a violation of 'similar things' repo directive."** | owner | **FIXED.** They are right about the direction of it: everywhere else in this app a single ✓ COMMITS — it is the confirming half of the pair every form wears, and its arming is what says something changed — while over a list of rows to choose between the same drawing meant "tick all of them", a press that writes nothing and leaves the reader still at the decision. `IconCheckAll` and `IconCloseAll` are two whole marks with daylight between them, not the icon sets' overlapping *done_all* whose second mark is a bare diagonal that reads as a slash at 24px in a 1.85 stroke. **The cross went with the tick and the report did not ask for that:** its job there is "untick all", and a double tick beside a single ✕ reads as "select all / cancel", which moves the confusion one control along instead of ending it. **A second site nobody had reported** — a board's ⋯ row that STARTS multi-select — wore the confirming tick for the same reason and is fixed with it. Guarded by `plural-is-not-confirm.test.jsx`, which asks that the plural glyphs are different drawings AND drawn twice, then names the three list-wide controls and reads their source |
| P2 | **"the tooltip prose 1st line repeats itself and the second line uses uncommon phrasing. this is not isolated. run a haiku subagent pass on all prose longer than 5 words and ask it to flag suspicion. then a sonnet subagent run will rewrite them."** | owner | **FIXED.** 964 English strings run over five words. Ten haiku passes flagged 26; a sonnet pass judges each flag and rewrites only what deserves it, against the house voice and the four length budgets (`.what` 160, `.how.N` 120, `.more` 420, an info dot 240). **The sweep is not exhaustive and the proof is the report itself:** `common.work.merge.info.body`, the string the owner was looking at, was NOT flagged — the brief quoted it as an example and the pass over its chunk still let it through, so it was added to the rewrite list by hand. Every English change needs its Bengali twin: `token-coverage.test.js` compares placeholders across the two, and a value changed on one side alone leaves the other saying the old thing. **14 English values and 13 Bengali twins landed** — one Bengali line already said what the corrected English now says, so it was left alone. **Two of the pass's calls are overruled and recorded:** `settings.backup.passphrase.prose` keeps "lose it and the archive is lost", because that is a consequence to the reader's data and not a restatement of "not recoverable"; and the merge note reads plainer as a verb ("Undo it from the bin") than as "keeps it undoable". **And a wording test had to be loosened rather than repaired:** `identity-panel.test.jsx` matched the literal phrase "bin holds the way back" and broke on a pass that changed no behaviour — which is the test the owner ruled out ("a test writer shouldn't even know about the fix"). It asks for the fact now: that the confirm says the merge is undoable and where from |
| P3 | **"the works say it is release order, and it was. but then i rectified a metadata problem in gardens of the moon (which released in 1999, not 2009 as my backup suggested). this should have automatically taken it to the front. but it didn't."** | owner | **FIXED. It never was release order.** `PersonCredits` orders by role then TITLE and `castWhere` by title alone, and neither shape carried a year at all — so the strip of nine Erikson novels opened Deadhouse Gates, Dust of Dreams, Gardens of the Moon, which is the alphabet reading like a series a reader half-remembers. Correcting a year could not move a tile because no tile had ever been placed by one. Both shapes now carry `books.published_year` / `movies.release_year`, and **`AppearanceStrip` sorts** — where the promise is printed, not in the two callers: a person's strip is what they PLAYED concatenated with what they MADE, two queries and two lists, so a sort in either leaves the join in neither order. An undated work goes LAST (0 is "not known", and sorting it numerically would open every strip claiming the works nobody has dated are the earliest); the sort is stable, so "what they played leads" survives inside one year. Guarded on both sides: a Go case asks the two reads for a dated work and an undated one across both kinds, and `release-order.test.jsx` asks the rendered strip, including the report itself — change one tile's year and it moves |
| P4 | **"another agent will be the planner for new features. all new plans will land in docs/plans. do a periodic sweep using a sonnet subagent and update roadmap accordingly."** | owner | **DONE — NOT A DEFECT but an instruction, and it needed a route before it could be an instruction.** `docs/plans/` had no way onto the roadmap: `features.json` carries only `overrides` keyed by ISSUE NUMBER, and the accepted and considered lists are generated from the tracker — so a feature nobody filed a ticket for could not be said to be coming. `features.json` gains a `manual[]` in the shape `bugs.json` has always had for a bug nobody filed, with a `plan` field naming the file it speaks for so the sweep can tell a shipped plan from an open one without matching on prose, and `shipped_in` so it retires the way everything else does. The instruction itself is in CLAUDE.md's "The plan queue"; a daily Routine runs the sweep |

## Fixed, with the commit that did it

| # | Defect | Fixed in |
|---|---|---|
| F1 | The ⋯ menu opened an empty card on six of twelve screens | `a94e141` |
| F2 | The character sheet's rows scrolled to a fold instead of opening an editor | `a94e141` |
| F3 | "Called here" and "Also called here" were two rows editing one fact | `a94e141` |
| F4 | Three identity sheets committed with a body "Save" button, not the standing pair | `a94e141`, `ecd65d4` |
| F5 | `PanelHost`'s tick never armed and never counted; its ✕ was never red | `ecd65d4` |
| F6 | Every card's ⋯ lacked `aria-haspopup` and `aria-expanded` | `ecd65d4` |
| F7 | The rail brand and the root crumb could not say "you are already here" | `d0304f4` |
| F8 | "Add another performer" was a text box, not the library's people | `c6b1ae3` |
| F9 | A claim made in three documents that `PanelHost` already drew the armed tick | `ecd65d4` (corrected in both docs) |
| F10 | `0d84411`'s claim that three cast writers never set `character_id` | `a94e141` (reverted, corrected in the audit) |

## Q. Found by the run itself, 6 September

| # | Defect | Source | Status |
|---|---|---|---|
| Q1 | The grab bar on a phone sheet did nothing when you pressed it | `make controls` at 390 | **FIXED.** The bar is a button, and N1 gave it a press that cycles the anchors precisely so a mouse could reach the gesture. It never ran. The bar is live from the FIRST pointerdown by design — it has nothing else to be — and the release read that liveness as "a drag happened", so every press settled at the anchor it was already on and the click that followed was swallowed as the drag's leftover. A release is a drag now only if the pointer travelled the same four pixels that make a touch in the body a drag rather than a tap on a row. **Why three layers all missed it:** the jsdom case fired `fireEvent.click` alone, which is the third of a press's three events and skips the two that make the rule hard; `make sheet-drag` had no press case at all; and `make controls` DID press it and could not see the answer, because its fingerprint carries no geometry and the bar's entire effect is a height. All three are repaired — the jsdom cases press the way a reader does, the browser probe presses the bar, and the fingerprint carries the sheet's height. `5bde71b` |

## R. Found by the work-rater, 6 September

A `claude-kit:work-rater` pass over `619eb05`, `feef70f` and `9b39771` scored the round
**6/10** and named seven things. Its score is recorded as given; the rows are what it
found.

| # | Finding | Status |
|---|---|---|
| R1 | `parseLinks` lost BOTH links on a line holding two addresses and a name — `a.com b.com \| Shop` became one dead non-URL chip, and the name went with it | **FIXED.** The head splits on whitespace first, always: that this field whitespace-splits is the invariant the pipe was chosen to protect, and reading the whole head as one address broke it. A name belongs to the last token on the line — the one the ` \| Name` was written against. `2853594` |
| R2 | `npm test` was red at the tip: `pack-citations.test.js` failed 14 skipped ≥ 14 found | **FIXED**, and it was never this work's failure — two of the planner's plan documents cite Go and JSX lines in the citation shape, and the guard asserted that such citations stay FEWER than pack ones, which is a ceiling on nobody's rule. The fear it names is a resolver that resolves nothing, and the case above it already says that. `5bde71b` |
| R3 | P4's roadmap was never updated: `features.json` had no `manual[]` key at all, so twelve plans were invisible to the page | **FIXED.** The route was built and the key was not added — the code read it, the file's own `_` block documented it, and it was not there. A sonnet sweep carded seven plans and left five out with reasons; `node scripts/roadmap-data.mjs --check` exits 0. `2853594` |
| R4 | The store→strip wiring for release order was unguarded — deleting the year from either map left the whole suite green | **FIXED.** Two cases render the actual screens from payloads shaped like the server's, one per map, and each fails when its own map drops the year. A person's strip and a character's are two shapes and two functions, so one guard says nothing about the other. `2853594` |
| R5 | A link's name reached one screen out of three: `PersonLinksDetail` ignored it, and the person/character ＋ had no name box | **FIXED.** `namedLinks` is the one function every screen calls; what genuinely differs per screen is passed in. `2853594` |
| R6 | Weak guards: the list-wide control case read 400 characters either side of a key and asserted only absences | **FIXED.** It balances outward to the declaration that draws something, and asks for the plural mark rather than only for the absence of the singular — a control drawing no glyph at all used to pass. `identity-panel.test.jsx`'s loosening stands: it was a wording test, which the owner ruled out. `2853594` |
| R7 | Gate misses inside the commits: no CHANGELOG for the prose sweep, no AI.md for the probe change, and the 390 half of `make controls` unreported | **FIXED**, each in its own commit rather than retroactively: `484a76e` (changelog), `0aaf2e7` (AI.md). The 390 pass then ran and found a real defect — see Q1 |

## S. The owner's reports of 6 September, sixth round

Two phone screenshots: a person's picture sub-sheet opening low, and Sinéad Cusack's
record with a broken-image mark on a work tile.

| # | Defect | Source | Status |
|---|---|---|---|
| S1 | **"the header bar is too thick (vertically). make it slimmer. by at least 30-40%"** | owner | **FIXED.** The bar was 44px of key inside 16px of padding, above a 44px strip that existed only because the mark was the only thing a thumb could grab. Moving the target to the whole header (S5) freed the strip to be what it draws — 18px around a 4px mark — and the bar itself is now 49px around its 44px key. **104px → 67px, 36%**, measured by `make sheet-drag`, which asks it as a rule rather than as a number: the bar may exceed its tallest key by the padding a rule needs and no more. The 44px floor under the KEY is the pack's and is untouched |
| S2 | **"this is a long popup, but it has a very low starting position. this is probably because it is inheriting the positioning of the picker. which is understandable, and is fine. but when there is no picker (only one available option), it makes no sense."** | owner | **FIXED.** A sub-surface opens INSIDE the same box by design — `.tp-subsheet`, so it cannot escalate to the viewport — so the panel element never changed, the drag effect never re-ran, and the sheet kept the height measured for whatever was in it first. It re-measures after every render of the surface now, and follows its content only while the reader has not placed it: a sheet dragged to 76% or 94% has been PUT there. The measurement changed too — natural is the chrome plus the body's own `scrollHeight`, not the sheet's `scrollHeight`, which the stylesheet's `max-height` had already capped so a long sheet measured as exactly the cap |
| S3 | **"the drag animation is clunky and not smooth, and it feels like it lags a little bit. is this not being handled by the client browser?"** | owner | **FIXED** with S6 — same cause |
| S4 | **"the character chip (delia sturridge) on the V for vendetta poster is a missing image glyph that looks like server has broke. it should be simply a random person glyph, as used in the actual delia sturridge character page."** | owner | **FIXED**, over three commits rather than the one this row first claimed. Every site branched on whether a PATH was stored and none on whether the file arrived, which are different questions and only the second is one the reader can see. `Face` owns the fallback and what genuinely differs — the address builder, and what stands in — is passed in. **The first commit said "all of them" and reached six of eleven**; `869c77c` corrected the styling half, and the row below closes the rest with an invariant rather than a count. `619eb05`, `869c77c`, `b3f041f` |
| S5 | **"the bar is too small to drag. the whole header bar should act as the bar. the bar is there just to make it intuitive."** | owner | **FIXED.** The mark is a sign and the header is the target: 67px of draggable top against the 44px a thumb needs. The keys inside the bar keep their press, by the rule that already let the mark itself be pressed — a pointer sequence that never travels four pixels is a press. `touch-action: pan-x` and not `none`, because the title and crumb scroll sideways under their fade and taking every gesture would freeze that |
| S6 | **"the animation is not just not-smooth. it introduces screen tears!!"** | owner | **FIXED.** The height was written once per POINTER EVENT, and a pointer stream arrives finer than a frame — so the browser was laying out the sheet and re-blurring the whole screen behind it several times inside one frame, and presenting halves of two of them. One write per animation frame now; the scrim's 10px backdrop blur stands down for the length of the gesture, because nobody is reading a blur while the thing in front of it is moving |

## T. The work-rater's second pass, 6 September

A fresh `claude-kit:work-rater` pass over the ten commits of the round scored it **7/10**,
up from the first pass's 6. Six findings; four were live.

| # | Finding | Status |
|---|---|---|
| T1 | The owner's own P1 report had no behavioural guard — the rater took the control off the merge screen entirely and the source scan stayed green | **FIXED.** `MergeScreen` is exported and rendered: the pair is asked to BE there, to wear two marks and not the singular one, and to tick the whole list when pressed. All three of the rater's mutations fail it now, including its own. `25a3dd8`'s successor |
| T2 | `namedLinks` had no test on two of the three screens that call it — mutating it so the reader's name never wins failed exactly one case, on the screen that already worked | **FIXED.** A case on the person's chips (`person-modal-icons`) and one on the global record's pills (`character-global`); the mutation fails both |
| T3 | A claim this round disproved was still in the code it edited: `castOf` says "the rows arrive in release order" and they arrive by TITLE, so a performer's face was the alphabetically-first work's | **FIXED.** Asked of the year rather than of an order this fold does not control. `a.year` was already on the row and unused |
| T4 | No recorded `make controls` 390 result after the grab-bar fix | **FIXED** by recording it — see the run below |
| T5 | Two byte-identical copies of the link writer (`identity.jsx`, person and character) | **FIXED.** `useLinkAdder` is the one copy; the endpoint is what each screen passes in |
| T6 | Sweep bookkeeping: the count in `2853594`'s message was wrong, and the exclusions lived only in that message — so the daily sweep re-litigates six files every night | **FIXED.** `docs/plans/README.md` names them, and the Routine reads that list first. 14 files, 7 carded, 7 skipped |

**The run T4 asked for**, `make controls` after `5bde71b`, both widths, exit 0:

- **1280** — 15 surfaces, 518 presses, 0 dead on each; `small` 0/0, `labelled` 0/0.
- **390** — 15 surfaces, 342 presses, 0 dead on each; `small` 187/326, `labelled` 9/56.
- **Character panel at 390: 23 controls, 0 dead** — the surface and the width that found Q1.

A note on the first rater's contrary report of a detached frame on Metadata: its run and
this one were concurrent, which it says itself ("two Chrome probes were resident", and its
390 pass then found no server at all). The class is real even so, and the fence now
re-walks a surface that loses its document once before failing the run.

## U. The work-rater's third pass, 6 September

A fresh pass over the sheet round scored it **6/10** and named eight things. Six were
live, and one of them was an overclaim in a commit message of mine.

| # | Finding | Status |
|---|---|---|
| U1 | S4 shipped with **no guard at all** — deleting `onError` left 3,388 tests green, on the one item of that round the owner had photographed | **FIXED.** `face-fallback.test.jsx`: a fired `error` must replace the picture with the glyph, with the SAME glyph an absent picture draws — the owner's own words — and the plate that goes with it. The rater's mutation fails all four cases `869c77c` |
| U2 | **"`Face` owns the fallback for all of them" was false.** Three sites still branched on the stored path, one of them the character page the owner named as the model; and `.is-empty` was computed from the path, so a broken picture drew an unstyled glyph | **FIXED.** All six sites now, and `is-empty` is drawn from what is on the SCREEN rather than from what is stored — the one place that knows is the thing drawing it `869c77c`, `b3f041f` |
| U3 | `npm run glossary:check` exited 1 at the tip — a shipped gate, red | **FIXED.** `make glossary`, and the check is in the closing gate list from here `869c77c` |
| U4 | The "not once the reader has placed it" guard survived its own mutation: the fixture placed the sheet at exactly the new content's first anchor | **FIXED.** The reader is placed at the TALLEST stop now, which is somewhere the rule under test would move them away from. Removing the rank check fails it `869c77c` |
| U5 | The blur stand-down had no test anywhere — half of S6 was assertion only | **FIXED.** The class is asserted on the sheet AND on the surface behind it, across a whole gesture `869c77c` |
| U6 | S5's press verb never moved: it stayed on the mark, now 18px — the affordance Q1 exists to give a mouse, stranded on something deliberately too small to hit | **FIXED.** The bar answers a press too. A press that lands on a control INSIDE the bar belongs to that control, which is how the ✕ keeps its own `869c77c` |
| U7 | The probe's re-walk appended to the findings its first, partial attempt had already pushed — double-counting a ratchet for a reason that is not about the app | **FIXED.** The surface's findings are truncated back to their mark before the second walk `869c77c` |
| U8 | The header slimming reaches the DESK panel too, and neither commit nor the departure entry said so | **NOT A DEFECT, and now recorded.** One component may not be two heights by viewport — that is the drift "similar things should act similarly" exists to stop — and what was removed is room around a 44px key rather than anything the pack draws. `docs/PLAN.md` §1.12 no longer says the header is built verbatim `869c77c` |

## V. The work-rater's fourth pass, 6 September

Scored **6/10**, and every one of its five findings held.

| # | Finding | Status |
|---|---|---|
| V1 | U2's repair had **no behavioural guard**: reverting all three converted sites left 3,393 of 3,394 green, and the one failure was a DOM-shape assertion that would still pass with `onError` deleted — U1's own lesson, one item over | **FIXED**, and not by writing three more per-site cases. The rule is about the tree: only a component that ASKS the picture — through `onError` — may draw the thing that stands in for one, and there are two, each listed with its reason. A third site drawing a silhouette has its own idea of when a picture is missing, and that idea is the defect. Plus a render case on the screen the owner named as the model `c742dab` |
| V2 | **"all six now" was a false denominator** — `PersonPortrait` (`credits.jsx`), the round face on Home, Quotes, film rows, search results and the quiz card, was still a bare `<img>`, with four more beside it | **FIXED**, though "eleven" was wrong too — see W2. The invariant above is what stops the next one, and reverting the widest of them fails it by name `c742dab` |
| V3 | The sentence the commit calls false was still in the register — S4 still read "`Face` owns the fallback for all of them" | **FIXED.** S4 now says six of eleven, and names all three commits `c742dab` |
| V4 | No U row named a commit, against the file's own rule | **FIXED** `c742dab` |
| V5 | `b3f041f` copied the truth instead of moving it: `is-empty` was still computed from the stored path in two places, one of which nothing read any more; and `.cast-face-btn .cast-face` beat `.cast-face.is-empty` on source order, so the stand-in's centring was dead inside the button | **FIXED.** The dead class is gone from both callers and the tie is broken with `:not(.is-empty)` `c742dab` |

**And one it asked me to check rather than found:** whether `fallback={null}` hides
something the reader needed. On four of the five slots nothing is right — they are
ornaments the design draws nothing for. On the fifth it was not: the person record's
photograph is a BUTTON that opens it full-screen, so a file that had gone left a 104px
control that does nothing, which is the defect `make controls` exists to catch arriving
through the back door. `Face` tells the caller now (`onBroken`) and the record falls back
to the same "no photograph" block a record without one draws. The judgement of whether a
picture failed stays in the one place; what a screen does about it is the screen's.

Two more things it found while reading, neither of them in its list: `identity.jsx` imported
`Silhouette` and never used it, and `PortraitBlock` — the one picture on the screen whose
dimensions it prints — had inherited `loading="lazy"` from `Face`'s default, which defers
the measurement the caption is waiting for. Both fixed.

## W. The work-rater's fifth pass, 6 September

Scored **5/10**, the lowest of the five, and it earned it: the first finding is the
owner's reported symptom, live, on a screen the commit said was covered.

| # | Finding | Status |
|---|---|---|
| W1 | **The Stats breakdown still drew a character's still as a bare `<img>`** — the owner's exact reported picture, unfixed — and the invariant written to stop it *could never catch it*: a still is a WORK's art, so `coverImgURL` builds it, and the rule policed `personImgURL` only | **FIXED.** The rule asks what a picture is OF now, read off the words this codebase uses for one, rather than which function built the address. Reverting the Stats row fails it by name |
| W2 | The invariant matched only the inline `src={…personImgURL(…)}` shape — putting the address in a `const` one line up walked straight past it, which is the shape seven of eight call sites already use | **FIXED.** A bare identifier is resolved one step through its own declaration, and the rule is shown a picture of each shape it must catch, so a pattern that quietly stops matching fails rather than going green |
| W3 | `fallback` shipped with no test — the same gap as U1, one commit later | **FIXED** in `ebea143`, which the rater saw uncommitted and called the right shape |
| W4 | `fallback={null}` left the person record's zoom button a 104px control that does nothing, opening a lightbox on the same dead file | **FIXED** in `ebea143` |
| W5 | `fallback={null}` is wrong on the Stats person tile, whose own comment says a name alone "reads as the one tile whose art failed to load" — which is exactly what a gap produces | **FIXED at the second attempt, and the first was marked FIXED while broken** — see X2. The caller asked for the silhouette with `fallback={undefined}`, and a destructuring default fires on `undefined`, so it got back the very `null` it was avoiding. It is a boolean now, which cannot be defaulted out from under its caller |
| W6 | `cast.jsx` still computed `is-empty` from the stored path; V5's "gone from both callers" was inaccurate | **FIXED**, and the two rules that read it now ask the box rather than the caller, so the class is gone rather than shadowed |
| W7 | No V row named a commit — the rule U4 was raised for, broken again one section later | **FIXED** |

**And the widened rule immediately found two more of its own:** a binned record's face
(`BinPage.jsx`) and a board's picture, in both places a board draws one (`boards.jsx`).
Both are the same question asked wrong — is a path stored — and both are converted, each
keeping its own stand-in, which is what `fallback` is for. Twelve sites, and the count is
now the invariant's to keep rather than a commit message's.

**One thing it named that is NOT this round's to change.** A person's record header draws
the pack's `Placeholder` when there is no photograph and a character's draws a silhouette
— two records, two answers. That is a difference between two EMPTY states, it predates
every commit here, and picking one is a design call with a visible result. Raised rather
than guessed at.

## X. The work-rater's sixth pass, 6 September

Scored **3/10**, the lowest yet, and rightly: its first finding is a crash I introduced
into the very row the round exists to fix.

| # | Finding | Status |
|---|---|---|
| X1 | **`StatsPage` referred to an identifier that does not exist in its scope** — `name={label \|\| ''}` in a row that had no `label`. It threw `ReferenceError` the moment a breakdown character had a stored still, which is precisely the case the change was made for, and worse than the torn page it replaced. It was in the committed bundle | **FIXED.** `r.name`, which is also the name the silhouette hashes on — `''` would have given every character the same face. **And the branch is rendered now**: nothing had ever drawn a breakdown row WITH a picture, because the fixture this suite runs on has no artwork. Restoring the crash fails that case with the ReferenceError itself |
| X2 | W5 was marked FIXED and was not: `fallback={undefined}` hits a destructuring default of `null`, so the Stats tile still drew the gap | **FIXED.** A boolean (`ornament`) cannot be defaulted out from under its caller. Two cases, one per kind of slot |
| X3 | Three more escapes from the invariant: `\bavatar\b` cannot match `avatar_path` (four live raw portraits behind it), `[^>]*?` cannot cross the `>` in an arrow function written before `src`, and `still` — the word the last commit used for the picture it was widening the rule to catch — was not in the list. `readdirSync` was also non-recursive | **FIXED.** The tag is read to its own end with `>` inside braces not counting; the vocabulary dropped its word boundaries and gained `still`; the walk recurses. Each shape it must catch is now shown to it, so a pattern that stops matching fails rather than passing |
| X4 | Deleting `.face-slot { display: contents }` and both `.stat-face-round` rules left the suite green | **FIXED.** Both are asserted, and deleting either fails its own case |
| X5 | `imgClass` is a leak, and `display: contents` silently voids `Face`'s `title` and `style` | **NOT A DEFECT here, and worth the note.** `imgClass` exists because two stylesheets dress the PICTURE rather than its box, which is a real difference and is what passing a fact in means; the stand-in is a whole element the caller supplies, with its own classes, so it neither needs nor wants that class. `title` and `style` on a `contents` slot: no `face-slot` caller passes either, and the four that pass `style` do not pass `face-slot` |

**And four more raw portraits it found:** the account avatar chips, in all four places one is
drawn (`Account.jsx` ×3, `App.jsx`). Their stand-in is the username's initial rather than a
silhouette, which is right — an account is not a person in the library — and is exactly what
`fallback` is for. Sixteen sites.

## Y. The work-rater's seventh pass, 6 September

Scored **5/10**. Its first finding is the sharpest of the whole loop: the crash the
previous pass caught had **two live siblings two lines up**, in the same span, and the fix
had been applied to one branch of three.

| # | Finding | Status |
|---|---|---|
| Y1 | The other two art branches in the same span — a work's cover and a person's portrait — had never been rendered by anything. Replacing either argument with a free identifier left 3,405 tests green: **X1's defect, live, two lines up** | **FIXED.** All three branches render now, and a free identifier in either of the other two fails its own case with the `ReferenceError` |
| Y2 | The headline fix's CALL SITE was unguarded — deleting `ornament={false}` left the suite green. The new case tested the component and never the caller, which is exactly the shape of the regression it was written for | **FIXED.** The Stats tile renders and its picture is failed, scoped to the tile because the breakdown rows draw the same component as an ORNAMENT — which is the distinction under test |
| Y3 | Five more walk-arounds of the invariant: a raw portrait **inside** an asker file (the exemption was per-FILE), a declaration broken across lines, `React.createElement('img')`, a spread, and a field outside the vocabulary | **FIXED, except the last, which is stated rather than closed.** The exemption belongs to the silhouette rule — a tag that draws a face and never listens for `error` is wrong wherever it is written, including in the two files that answer for everyone else. The decl lookup reads flattened text; a tag with no `src={}` is read entire, which catches a spread; `createElement('img')` has its own case. A field this codebase has never used for a face cannot be caught by a vocabulary, and the note says so |
| Y4 | The CSS assertion was a presence check — rewriting the rule to `width: 4%; display: inline` passed, which `css-cascade.js`'s own header condemns | **FIXED.** The values are asserted |
| Y5 | Four copies of one verb: the account chips duplicated `UserAvatar`'s body instead of calling it | **FIXED.** `avatar.jsx` is a leaf module because of where `Face` lives — `App.jsx` already imports `Account.jsx`, so importing back is a cycle, and `ui.jsx` cannot hold it either |
| Y6 | The profile card still read the stored path: a gone avatar said "Change photo" and offered a Remove key for a picture that is not there | **FIXED**, through the same `onBroken` the person record already used |
| Y7 | `credits.jsx` still described a `fallback` prop that no longer exists | **FIXED** |
| Y8 | `CHANGELOG.md` stated one behaviour for sixteen sites that have four | **FIXED.** It says what each kind of slot falls back to |

**The vocabulary's boundary, stated rather than widened.** `thumb` was briefly added and
caught the two image PICKERS — remote candidates a reader is choosing between, where a
broken thumbnail says something true about that candidate rather than about the library.
Out again, deliberately, rather than converting those to hide a signal.

## Z. The work-rater's eighth pass, 6 September

Scored **6/10**. Its first finding overturns a conclusion I had just written into `AI.md`,
and it is right.

| # | Finding | Status |
|---|---|---|
| Z1 | **The class `afd3c33` declared unmechanisable was live in two screens.** `Library.jsx` and `Movies.jsx` each had a row's Delete key calling `setAsking`, which is bound in their PARENT — reachable buttons, both throwing when pressed. And Babel is already in `node_modules`, so a scope check finds them in a second | **FIXED, and the note is corrected.** Both call the `remove` prop the parent binds to `setAsking`, so a row puts the same question a card does. `test/pure/no-free-names.test.js` is the mechanical control: it catches both of these AND the shipped `StatsPage` crash, each by file and line. My argument had weighed two ways of RUNNING the code and never considered reading it — wrong in the one direction the standing instruction forbids. **And a scope check knows the NAME is missing, not that the button works** — the owner's standard is "is the button clickable (for all buttons)?" — so `test/dom/table-row-delete.test.jsx` renders both tables and presses the key: four cases, all four red when either row is reverted |
| Z2 | The avatar extraction and the profile card's `gone` state shipped with no test: dropping `onBroken`, swapping the initial for a silhouette, or reverting the Remove key each left 3,409 green | **FIXED.** `account-avatar.test.jsx`, five cases; both mutations fail |
| Z3 | Y3's spread claim was broader than the code — only the inline object was closed, and `onError={undefined}` or the word in a comment passed | **FIXED.** A spread of a variable is resolved like a bare `src`; comments are stripped from the tag before it is read; an `onError` bound to `undefined` or `null` is not asking |
| Z4 | Every `<img>` in `src/` scores zero on the vocabulary today, so the file scan is carried entirely by the synthetic shapes | **NOT A DEFECT — that is the rule working.** No site draws a raw face any more, which is the point; the synthetic shapes are what keep the pattern honest, and each is a shape that actually escaped once |
| Z5 | A duplicate `authors` key in the stats fixture, warned on by esbuild every run | **FIXED** |
| Z6 | `.stat-face-round svg` was unasserted — the rule listed the picture and the glyph together and only the picture was asked for | **FIXED.** Split into a block each, so a guard naming one is not silently asserting the other |

**The syntax error this pass also caught, before any rater did.** The first version of the
`Library.jsx` fix put its explanation in JSX ATTRIBUTE position, where `{/* … */}` is not
legal. The new scope check failed with the parse error and named the line — which is a
better argument for it than any of the above.

## AA. The work-rater's ninth pass, 6 September

Scored **6/10**, against `b60c75c`. Two of its seven were already closed by `66a31a0`,
which landed while it was reading; the other five were open and all five were real.

| # | Finding | Status |
|---|---|---|
| AA1 | **The scope check passes green reading zero files.** Narrow the walk's extension and both cases stay green — the second tests the ANALYSER on strings, and nothing tied it to the tree | **FIXED.** A third case asserts the walk found more than 60 files and that `App.jsx` is among them. Blinding the walk fails it by name. Same guard `glyphs-are-drawn.test.js` has carried since it was written |
| AA2 | **Eight names on the allow-list were app-shaped.** `open` is this app's commonest prop, and `window.open` is truthy — so a child reading a missing `open` renders WRONG rather than throwing, which is the parent/child shape the file exists for. `process` was worse: Vite does not define it in a browser bundle, so it is not a global here at all | **FIXED.** `open`, `close`, `print`, `screen`, `alert`, `confirm`, `prompt` and `process` are off the list, and removing all eight changed nothing — no file uses any of them bare, so they were carrying no code and hiding a class. A synthetic case shows the check a child reading its parent's `open`, and putting the name back fails it |
| AA3 | **Z2 said FIXED for three mutations and covered two.** `Account.jsx`'s `gone` gate and the Remove key were untested — nothing referenced `AvatarRow` or `account.photo.remove` | **FIXED.** Five cases on the photo card, reached through `Profile` the way the screen reaches it. Four mutations fail it: dropping the `onBroken` wiring, `has` ignoring `gone`, the effect that resets `gone` per path, and `remove()` not asking the server |
| AA4 | Nothing pressed either Delete key in the commit | **ALREADY FIXED** by `66a31a0`, which landed mid-rating |
| AA5 | `CHANGELOG.md` untouched though the fix is user-visible | **ALREADY FIXED** by `66a31a0` |
| AA6 | **`onError={() => {}}` passed the guard**, and `.stat-face-round svg` was asserted for `width` alone — `height`, `object-fit` and `display` were all deletable green | **FIXED.** The handler is now read by matching braces and then asked whether it says anything, so a no-op body this list has not thought of still has to argue its case; three empty shapes are shown to it, and a one-line handler is shown to it too, so the rule stays fixable. The glyph's block is asked for the same four the picture's is |
| AA7 | `@babel/parser` and `@babel/traverse` were undeclared transitive imports, and `AI.md` called it "a dependency of zero" | **FIXED.** Both declared in `devDependencies` at `^7.29.7`; the note says the truth — no download, because Vite's React plugin already brings them, but declared anyway, since a transitive dependency is a fact about somebody else's package.json rather than a promise |

**The one that is worth reading twice is AA2.** Eight entries on an allow-list, none of
them used, every one of them a word this app also uses for its own things — a guard that
had been made weaker by a list nobody was checking against the code it was excusing.

## Withdrawn claims

Kept because the pattern matters more than any one of them.

- **"Dead controls went 44 → 19 → 1 → 0."** Desktop only. The phone pass was never run. See D2.
- **"Three of the four writers of `work_cast` never set `character_id`."** All four already
  called `LinkCastRow`. The mutation that seemed to prove otherwise was detecting damage
  the fix itself made possible.
- **"44px is stated in CLAUDE.md."** It is not; CLAUDE.md contains no "44". The number is
  the design pack's.
- **"`--type-ui-14` exists."** It is not on the scale, and `typescale.test.js` names it as
  its example typo. I had grepped for the token's USE and read my own new line back.
